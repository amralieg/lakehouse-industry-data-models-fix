**Model Quality Score: 92.96/100**

**PRIORITY 1 — rename_attribute: compliance.regulatory_agency** — rename column parent_regulatory_agency_regulatory_agency_id to parent_regulatory_agency_id because current name contains a redundant duplicated table stem
**PRIORITY 1 — rename_attribute: treatment.chemical_inventory** — rename column chemical_chemical_inventory_id to parent_chemical_inventory_id because current name contains a redundant duplicated table stem and lacks a business-meaningful self-ref label
**PRIORITY 1 — rename_attribute: treatment.filter_unit** — rename column paired_filter_unit_id to paired_with_filter_unit_id because current column name is ambiguous with the PK stem — actually acceptable, leave
**PRIORITY 1 — rename_attribute: quality.water_sample** — rename column parent_sample_water_sample_id to parent_water_sample_id because current name contains a redundant duplicated stem
**PRIORITY 2 — connect_table: asset.asset_class** — add column parent_asset_class_id (BIGINT) with FK to asset.asset_class.asset_class_id — asset classes are hierarchical (e.g., Pump → Centrifugal Pump) and require parent linkage
**PRIORITY 2 — connect_table: asset.criticality_rating** — add column asset_class_id (BIGINT) with FK to asset.asset_class.asset_class_id — criticality ratings apply to asset classes; currently isolated with no outbound FKs
**PRIORITY 2 — connect_table: metering.meter_size_type** — add column asset_class_id (BIGINT) with FK to asset.asset_class.asset_class_id — meter size types classify meter assets and need linkage to the asset class registry
**PRIORITY 2 — connect_table: treatment.facility** — add column territory_id (BIGINT) with FK to service.territory.territory_id — facilities operate within service territories; facility currently has zero outbound FKs while many domains reference it
**PRIORITY 2 — connect_table: treatment.facility** — add column location_id (BIGINT) with FK to asset.location.location_id — a treatment facility must have a physical location
**PRIORITY 3 — connect_table: quality.contaminant** — add column contaminant_limit_id (BIGINT) — skip, would create cycle; instead: leave contaminant as reference
**PRIORITY 3 — connect_table: customer.person** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — persons are typically associated with an account; currently person only links to service_address
**PRIORITY 3 — connect_table: customer.organization** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — organizations need account linkage for commercial/industrial customers
**PRIORITY 3 — connect_table: billing.payment** — add column billing_account_id (BIGINT) with FK to billing.billing_account.billing_account_id — payments must be tied to the account being paid; currently payment only links to payment_plan
**PRIORITY 3 — connect_table: billing.payment** — add column payment_method_type or add invoice_id (BIGINT) with FK to billing.invoice.invoice_id — payments should link to the invoice they settle
**PRIORITY 3 — connect_table: quality.analytical_result** — add column water_sample_id (BIGINT) with FK to quality.water_sample.water_sample_id — analytical results must be tied to the sample analyzed
**PRIORITY 3 — connect_table: quality.analytical_result** — add column sampling_point_id (BIGINT) with FK to quality.sampling_point.sampling_point_id — analytical results must reference where the sample was drawn
**PRIORITY 4 — connect_table: distribution.pressure_zone** — add column territory_id (BIGINT) with FK to service.territory.territory_id — pressure zones sit within service territories; currently only links up to facility
**PRIORITY 4 — connect_table: distribution.dma** — add column pipe_main_id or ensure territory linkage exists — dma links only to pressure_zone and compliance_permit but not to territory; add territory_id (BIGINT) with FK to service.territory.territory_id
**PRIORITY 4 — connect_table: wastewater.manhole** — add column sewer_network_id (BIGINT) with FK to wastewater.sewer_network.sewer_network_id — manholes are physical components of a sewer network; currently manhole has no outbound network linkage
**PRIORITY 4 — connect_table: metering.meter_size_type** — currently only has self-ref primary_replacement_meter_size_type_id and inbound refs — verify not isolated; leave as reference table
**PRIORITY 4 — connect_table: service.territory** — verify service.territory has outbound linkage; it currently only links to treatment.facility — add parent_territory_id (BIGINT) with FK to service.territory.territory_id — territories can have parent/child hierarchies (region → district)
**PRIORITY 4 — connect_table: compliance.compliance_permit** — add column service_territory_id (BIGINT) with FK to service.territory.territory_id — permits often scope to a territory; currently only links to agency and facility
**PRIORITY 5 — connect_table: billing.rate_component** — add column service_class_id (BIGINT) with FK to service.service_class.service_class_id — rate components are tiered by service class (residential/commercial/industrial)
**PRIORITY 5 — connect_table: service.program_enrollment** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — enrollment must identify the enrolling customer
**PRIORITY 5 — connect_table: service.program_enrollment** — add column premise_id (BIGINT) with FK to customer.premise.premise_id — conservation programs are premise-specific
**PRIORITY 5 — connect_table: metering.read** — add column meter_id (BIGINT) with FK to metering.meter.meter_id — reads should tie directly to the meter, not only to installation and endpoint
**PRIORITY 5 — connect_table: distribution.flow_reading** — add column reading_timestamp validation — no action; already has FKs
**PRIORITY 5 — connect_table: treatment.process_reading** — add column process_unit_id (BIGINT) with FK to treatment.process_unit.process_unit_id — process readings must reference the process unit generating them
**PRIORITY 5 — connect_table: treatment.process_reading** — add column chemical_dose_event_id — skip, dose_event already references process_reading; keep
**PRIORITY 6 — connect_table: wastewater.effluent_discharge_event** — add column industrial_user_permit_id (BIGINT) with FK to wastewater.industrial_user_permit.industrial_user_permit_id — industrial discharges tie to IUP
**PRIORITY 6 — connect_table: wastewater.industrial_user_permit** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — industrial users are customer accounts
**PRIORITY 6 — connect_table: compliance.violation** — add column enforcement_action_id — skip, enforcement_action already references violation; keep direction
**PRIORITY 6 — connect_table: compliance.dmr** — add column mor_id or upstream data source — skip; keep
**PRIORITY 7 — connect_table: service.tariff** — add column service_offering_id (BIGINT) with FK to service.offering.offering_id — tariffs apply to specific offerings
**PRIORITY 7 — connect_table: service.service_rate_schedule** — verify service_class_id and tariff_id already present; no action
**PRIORITY 7 — connect_table: asset.registry** — add column criticality_rating_id (BIGINT) with FK to asset.criticality_rating.criticality_rating_id — registered assets should carry a criticality rating
**PRIORITY 7 — connect_table: asset.work_order** — add column failure_record_id (BIGINT) with FK to asset.failure_record.failure_record_id — corrective work orders originate from failure records
**PRIORITY 8 — connect_table: quality.contaminant** — add self-ref parent_contaminant_id (BIGINT) with FK to quality.contaminant.contaminant_id — contaminant groups have parent/child relationships (e.g., THMs → chloroform)
**PRIORITY 8 — connect_table: distribution.pipe_main** — add column parent_pipe_main_id (BIGINT) with FK to distribution.pipe_main.pipe_main_id — mains have upstream/downstream topology relationships
**PRIORITY 8 — connect_table: wastewater.sewer_network** — add column parent_sewer_network_id (BIGINT) with FK to wastewater.sewer_network.sewer_network_id — sewer networks have hierarchical basin/subbasin structure
**PRIORITY 8 — connect_table: distribution.pressure_zone** — add column parent_pressure_zone_id (BIGINT) with FK to distribution.pressure_zone.pressure_zone_id — pressure zones nest hierarchically
**PRIORITY 9 — connect_table: customer.complaint** — the complaint table has 25+ outbound FKs; review whether some (asset.registry, distribution.hydrant, distribution.network_valve) should be attributes rather than direct FKs — no action needed if intentional
**PRIORITY 9 — connect_table: billing.adjustment** — the adjustment table has 22+ outbound FKs indicating scope creep; consider narrowing — no direct action, flag for review
**PRIORITY 10 — connect_table: asset.acquisition** — remove FK to customer.premise (acquisitions are procurement events unrelated to a specific customer premise) — remove_fk: asset.acquisition — remove FK on column premise_id because acquisitions are asset procurement events not tied to customer premises
**PRIORITY 10 — remove_fk: billing.invoice_line** — remove FK on column water_sample_id because invoice lines bill consumption/service, not lab samples
**PRIORITY 10 — remove_fk: billing.adjustment** — remove FK on column water_sample_id because billing adjustments are not tied to lab samples
**PRIORITY 10 — remove_fk: customer.complaint** — remove FK on column water_sample_id because complaints are customer-reported issues, not lab records
**PRIORITY 10 — remove_fk: metering.interval_consumption** — remove FK on column registry_id (asset.registry) because interval consumption is meter data, not general asset registry data — installation already provides the meter linkage
**PRIORITY 10 — remove_fk: wastewater.sso_event** — remove FK on column water_source_id because SSOs are collection-system events unrelated to potable water sources
**PRIORITY 11 — connect_table: service.point** — verify service.point → customer.service_address linkage; currently service.point does NOT link to service_address — add column service_address_id (BIGINT) with FK to customer.service_address.service_address_id — service points are located at service addresses
**PRIORITY 11 — connect_table: metering.installation** — add column service_address_id (BIGINT) with FK to customer.service_address.service_address_id — installations occur at specific service addresses
**PRIORITY 11 — connect_table: distribution.service_line** — verify link to premise — add column premise_id (BIGINT) with FK to customer.premise.premise_id — service lines physically serve premises
**PRIORITY 12 — connect_table: quality.sampling_schedule** — add column contaminant_id (BIGINT) with FK to quality.contaminant.contaminant_id — sampling schedules target specific contaminants
**PRIORITY 12 — connect_table: compliance.corrective_action** — add column dmr_id or mor_id linkage — currently corrective_action links to violation and enforcement_action but not to originating report; add mor_id (BIGINT) with FK to compliance.mor.mor_id
**PRIORITY 13 — connect_table: treatment.water_source** — add column territory_id already exists via service.territory; verify — currently water_source links to service.territory and compliance.compliance_permit; add pressure_zone_id (BIGINT) with FK to distribution.pressure_zone.pressure_zone_id — skip if not needed
**PRIORITY 13 — connect_table: wastewater.wwtp** — add column receiving_water_source_id (BIGINT) with FK to treatment.water_source.water_source_id — WWTPs discharge to receiving waters
**PRIORITY 14 — connect_table: service.offering** — add column service_class_id (BIGINT) with FK to service.service_class.service_class_id — service offerings apply to service classes; note current linkage is service_class → offering, verify direction and adjust if inverse is more natural

Other known issues from static analysis (4):
  - Attribute metering.read_route.route_description has unrecognized data type 'TEXT' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Table treatment.facility has only 1 attribute(s) — likely incomplete or should be merged
  - Product 'customer.complaint' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - 35 attribute(s) match person-data patterns but lack pii_ tags

Deterministic score: 92.96/100 (LLM assessment: 72/100)
