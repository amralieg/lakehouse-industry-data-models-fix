**Model Quality Score: 92/100**

**Static Analysis Findings (1 actionable):**
  - [SA:denormalized_natural_key] Product 'talent.residual_payment' has both FK 'payment_id' and natural key 'payment_number' for entity 'payment'. The natural key is redundant — use the FK join instead.

**PRIORITY 1 — connect_table: compliance.regulatory_obligation** — add column broadcast_license_id (BIGINT) with FK to compliance.broadcast_license.broadcast_license_id — regulatory obligations must anchor to the broadcast license they enforce for FCC/Ofcom compliance tracking.
**PRIORITY 1 — connect_table: content.rating** — add column regulatory_obligation_id (BIGINT) with FK to compliance.regulatory_obligation.regulatory_obligation_id — MPA/TV ratings must trace to the regulatory framework that governs them.
**PRIORITY 1 — connect_table: distribution.device_type** — add column ott_platform_id (BIGINT) with FK to distribution.ott_platform.ott_platform_id — device types must be scoped to platform support matrices for entitlement checks.
**PRIORITY 2 — connect_table: audience.demographic_segment** — add column regulatory_obligation_id (BIGINT) with FK to compliance.regulatory_obligation.regulatory_obligation_id — demographic segmentation (COPPA under-13, GDPR minors) must reference governing obligation.
**PRIORITY 2 — connect_table: audience.segment_membership** — add column sweeps_period_id (BIGINT) with FK to audience.sweeps_period.sweeps_period_id — membership snapshots must be tied to the measurement period for point-in-time reporting.
**PRIORITY 2 — connect_table: content.genre** — add column regulatory_obligation_id (BIGINT) with FK to compliance.regulatory_obligation.regulatory_obligation_id — genre classification drives content standards obligations (news, kids, political).
**PRIORITY 2 — connect_table: talent.guild_affiliation** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — the guild itself (SAG-AFTRA, WGA, DGA) is a partner organization and must resolve to a partner record.
**PRIORITY 2 — connect_table: talent.talent_profile** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — loan-out companies / production entities associated with talent must connect to partner master.
**PRIORITY 2 — connect_table: scheduling.channel** — add column ott_platform_id (BIGINT) with FK to distribution.ott_platform.ott_platform_id — linear channels simulcast on OTT must resolve their streaming platform.
**PRIORITY 2 — connect_table: scheduling.daypart** — add column sweeps_period_id (BIGINT) with FK to audience.sweeps_period.sweeps_period_id — daypart pricing/measurement varies by sweeps window.
**PRIORITY 3 — connect_table: billing.cycle** — add column billing_account_id (BIGINT) with FK to billing.billing_account.billing_account_id — cycles must anchor to the account they invoice against.
**PRIORITY 3 — connect_table: billing.payment_method** — add column payment_instrument_id (BIGINT) with FK to subscriber.payment_instrument.payment_instrument_id — subscriber payment instruments realize billing payment methods.
**PRIORITY 3 — connect_table: compliance.broadcast_license** — add column territory_id (BIGINT) with FK to rights.territory.territory_id — broadcast licenses are territorial and must resolve to a licensed territory.
**PRIORITY 3 — connect_table: compliance.broadcast_license** — add column regulatory_obligation_id (BIGINT) with FK to compliance.regulatory_obligation.regulatory_obligation_id — the license itself is issued under a specific regulatory obligation.
**PRIORITY 3 — connect_table: compliance.content_rating** — add column rating_id (BIGINT) with FK to content.rating.rating_id — compliance content rating record must resolve to the master rating code.
**PRIORITY 3 — connect_table: content.acquisition** — add column acquisition_deal_id (BIGINT) with FK to partner.acquisition_deal.acquisition_deal_id — content acquisitions execute against a partner acquisition deal.
**PRIORITY 3 — connect_table: content.acquisition** — add column license_agreement_id (BIGINT) with FK to rights.license_agreement.license_agreement_id — every content acquisition is governed by a license agreement.
**PRIORITY 3 — connect_table: distribution.streaming_endpoint** — add column delivery_channel_id (BIGINT) with FK to distribution.delivery_channel.delivery_channel_id — endpoints deliver via a specific delivery channel technology.
**PRIORITY 3 — connect_table: rights.territory** — add column parent_territory_id (BIGINT) self-referencing FK to rights.territory.territory_id — territories require hierarchical rollup (country→region→world).
**PRIORITY 3 — connect_table: sales.sales_agency** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — agencies are external organizations and must resolve to partner master.
**PRIORITY 3 — connect_table: sales.advertiser** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — advertisers are external commercial partners and must be modeled through partner master.
**PRIORITY 3 — connect_table: talent.talent_agency** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — talent agencies are partner organizations.
**PRIORITY 3 — connect_table: distribution.distribution_partner** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — distribution partners specialize the partner master and must reference it.
**PRIORITY 3 — connect_table: distribution.ott_platform** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — OTT platforms operated by external parties must resolve to a partner.
**PRIORITY 4 — connect_table: subscriber.household** — add column subscription_plan_id (BIGINT) with FK to subscriber.subscription_plan.subscription_plan_id — household-level plan enrollment must be trackable.
**PRIORITY 4 — connect_table: subscriber.viewer_profile** — add column household_id (BIGINT) with FK to subscriber.household.household_id — viewer profiles roll up to household for parental controls and shared entitlements.
**PRIORITY 4 — connect_table: audience.panel** — add column demographic_segment_id (BIGINT) with FK to audience.demographic_segment.demographic_segment_id — panels are stratified by demographic composition.
**PRIORITY 4 — connect_table: production.script** — add column talent_profile_id (BIGINT) with FK to talent.talent_profile.talent_profile_id — writers of scripts must resolve to talent records.
**PRIORITY 4 — connect_table: scheduling.program_schedule** — add column daypart_id (BIGINT) with FK to scheduling.daypart.daypart_id — schedules are organized by daypart for programming logic.
**PRIORITY 4 — connect_table: rights.holder** — add column license_agreement_id (BIGINT) with FK to rights.license_agreement.license_agreement_id — holders are counterparties on specific license agreements.
**PRIORITY 4 — connect_table: sales.sales_account** — add column sales_agency_id (BIGINT) with FK to sales.sales_agency.sales_agency_id — sales accounts are frequently agency-managed.
**PRIORITY 4 — connect_table: distribution.delivery_channel** — add column distribution_partner_id (BIGINT) with FK to distribution.distribution_partner.distribution_partner_id — delivery channels are operated by distribution partners.
**PRIORITY 4 — connect_table: compliance.eas_log** — add column delivery_channel_id (BIGINT) with FK to distribution.delivery_channel.delivery_channel_id — EAS alerts are dispatched over specific delivery channels.
**PRIORITY 4 — connect_table: talent.role** — add column content_episode_id (BIGINT) with FK to content.content_episode.content_episode_id — roles are scoped to a specific episode/production context.
**PRIORITY 5 — connect_table: compliance.public_inspection_file** — add column partner_id (BIGINT) with FK to partner.partner.partner_id — PIF ownership is at the station/partner level.
**PRIORITY 5 — connect_table: production.milestone** — add column shoot_schedule_id (BIGINT) with FK to production.shoot_schedule.shoot_schedule_id — production milestones tie to specific shoot schedules.
**PRIORITY 5 — connect_table: production.crew_assignment** — add column role_id (BIGINT) with FK to talent.role.role_id — crew assignments require a role definition.
**PRIORITY 5 — connect_table: subscriber.offer** — add column subscription_plan_id (BIGINT) with FK to subscriber.subscription_plan.subscription_plan_id — offers promote specific plans.
**PRIORITY 5 — connect_table: audience.viewership_record** — add column subscription_id (BIGINT) with FK to subscriber.subscription.subscription_id — viewership tied to a subscription enables ARPU/engagement analytics.
**PRIORITY 5 — connect_table: billing.invoice_line** — add column subscription_invoice_id (BIGINT) with FK to billing.subscription_invoice.subscription_invoice_id — subscription invoice lines must reference their parent invoice variant.

Other known issues from static analysis (6):
  - Table audience.guarantee has 2 FK columns pointing to scheduling.channel (channel_id, guarantee_network_channel_id) (generic/unlabeled: channel_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table sales.proposal has 2 FK columns pointing to sales.advertiser (advertiser_id, proposal_sales_advertiser_id) (generic/unlabeled: advertiser_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table audience.segment_membership has 2 FK columns pointing to audience.audience_profile (audience_profile_id, segment_profile_audience_profile_id) (generic/unlabeled: audience_profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 1 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 116 attribute(s) match person-data patterns but lack pii_ tags
  - 1 attribute(s) have pipe-enum value_regex with >8 values — consider creating a reference product instead

Deterministic score: 92/100 (LLM assessment: 72/100)
