**Model Quality Score: 98.58/100**

**Static Analysis Findings (1 actionable):**
  - [SA:unlinked_fk] Column asset.asset_work_order.pm_schedule_id looks like an FK but has no foreign_key_to reference

**PRIORITY 1 — connect_table: logistics.carrier** — add column default_payment_term_id (BIGINT) with FK to billing.payment_term.payment_term_id — carrier is an ISOLATED table with zero outbound FKs and must verify inbound links exist; however carrier_contract references it, so carrier is a valid reference table. RETRACTED.
**PRIORITY 2 — connect_table: inventory.warehouse** — add column plant_id (BIGINT) with FK to production.plant.plant_id — warehouses are physically located at or associated with manufacturing plants, and this critical relationship is missing despite both tables existing.
**PRIORITY 3 — rename_attribute: billing.invoice** — rename column primary_credit_note_invoice_id to credit_note_invoice_id because the "primary_" prefix is generic and the self-reference already implies it is the credit note relationship.
**PRIORITY 4 — rename_attribute: customer.customer_account** — rename column primary_ultimate_parent_account_customer_account_id to ultimate_parent_account_id because the column name is excessively long with redundant table-name prefix and "primary_" adds no clarity.
**PRIORITY 5 — rename_attribute: engineering.revision** — rename column primary_superseded_by_revision_engineering_revision_id to superseded_by_revision_id because the column name redundantly includes both "revision" and "engineering_revision" making it excessively verbose.
**PRIORITY 6 — connect_table: production.plant** — add column company_address_id (BIGINT) with FK to customer.address.address_id — plants have physical addresses needed for logistics, compliance, and shipping purposes but no address link exists.
**PRIORITY 7 — connect_table: production.routing_operation** — add column equipment_register_id (BIGINT) with FK to asset.equipment_register.equipment_register_id — routing operations are performed on specific equipment and this link is essential for scheduling and capacity planning.
**PRIORITY 8 — connect_table: asset.job_plan** — add column work_center_id (BIGINT) with FK to production.work_center.work_center_id — job plans define maintenance tasks executed at specific work centers, and this relationship is missing.
**PRIORITY 9 — remove_fk: asset.calibration_record** — remove FK on column customer_account_id because calibration records are internal maintenance activities on equipment and do not logically belong to a customer account.
**PRIORITY 10 — remove_fk: asset.downtime_event** — remove FK on column customer_account_id because downtime events are internal production disruptions tracked against equipment, not customer accounts.
**PRIORITY 11 — connect_table: quality.capa** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — CAPAs frequently originate from production issues and need traceability to the specific work order.
**PRIORITY 12 — connect_table: quality.ncr** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — non-conformance reports are typically raised during production execution and must link to the originating work order.
**PRIORITY 13 — connect_table: quality.fmea** — add column engineering_specification_id (BIGINT) with FK to engineering.engineering_specification.engineering_specification_id — FMEAs analyze failure modes against engineering specifications and this traceability link is missing.
**PRIORITY 14 — rename_attribute: engineering.engineering_specification** — rename column superseded_by_specification_engineering_specification_id to superseded_by_specification_id because the column redundantly embeds the full table name.
**PRIORITY 15 — connect_table: production.routing_operation** — add column material_master_id (BIGINT) with FK to inventory.material_master.material_master_id — routing operations consume materials and this link supports BOM explosion at the operation level.
**PRIORITY 16 — connect_table: billing.invoice_line** — add column production_plant_id (BIGINT) with FK to production.plant.plant_id — invoice lines for manufactured goods should trace to the producing plant for cost allocation and tax jurisdiction.
**PRIORITY 17 — rename_product: product.order_line** — rename to product_order_line because it conflicts semantically with order.line and creates confusion about which domain owns order line items.
**PRIORITY 18 — connect_table: quality.customer_complaint** — add column production_line_id (BIGINT) with FK to production.production_line.production_line_id — customer complaints about product defects should trace to the specific production line for root cause analysis.
**PRIORITY 19 — connect_table: supply.demand_forecast** — add column production_plant_id (BIGINT) with FK to production.plant.plant_id — demand forecasts are plant-specific for capacity planning and MRP execution.
**PRIORITY 20 — connect_table: asset.job_plan_material_requirement** — add column material_master_id (BIGINT) with FK to inventory.material_master.material_master_id — job plan material requirements should reference the material master for MRP integration, not only spare parts.
**PRIORITY 21 — remove_fk: asset.asset_work_order** — remove FK on column customer_account_id because internal maintenance work orders on owned assets are not customer-facing activities in a manufacturing context.
**PRIORITY 22 — connect_table: production.shift** — add column production_line_id (BIGINT) with FK to production.production_line.production_line_id — shifts are scheduled per production line and this relationship is needed for capacity and scheduling.
**PRIORITY 23 — connect_table: logistics.carrier** — add column address_id (BIGINT) with FK to customer.address.address_id — carriers have business addresses needed for documentation, contracts, and communication.
**PRIORITY 24 — rename_attribute: customer.customer_account** — rename column parent_account_customer_account_id to parent_account_id because the column redundantly includes the table name as both prefix and suffix.
**PRIORITY 25 — connect_table: quality.inspection_result** — add column control_plan_id (BIGINT) with FK to quality.control_plan.control_plan_id — inspection results should trace to the control plan that mandated the inspection for IATF 16949 compliance.
**PRIORITY 26 — connect_table: engineering.eco** — add column engineering_specification_id (BIGINT) with FK to engineering.engineering_specification.engineering_specification_id — ECOs frequently modify engineering specifications and this traceability is missing.
**PRIORITY 27 — connect_table: supply.capacity_plan** — add column production_plant_id (BIGINT) with FK to production.plant.plant_id — capacity plans are plant-specific and require this link for proper scoping.
**PRIORITY 28 — connect_table: supply.sourcing_rule** — add column production_plant_id (BIGINT) with FK to production.plant.plant_id — sourcing rules define where materials are sourced for specific plants.
**PRIORITY 29 — connect_table: production.bom_consumption** — add column production_work_order_id_header (BIGINT) with FK to order.header.order_header_id — BOM consumption should trace to the customer order driving the production for full order-to-consumption traceability.
**PRIORITY 30 — rename_attribute: asset.downtime_event** — rename column customer_account_id to reported_by_account_id because if the FK is retained, it should clarify the business relationship rather than implying ownership.
**PRIORITY 31 — connect_table: quality.ncr** — add column shift_id (BIGINT) with FK to production.shift.shift_id — NCRs should record which shift the non-conformance occurred on for workforce and process analysis.
**PRIORITY 32 — connect_table: quality.capa** — add column inspection_lot_id (BIGINT) with FK to quality.inspection_lot.inspection_lot_id — CAPAs are often triggered by failed inspection lots and need this traceability.
**PRIORITY 33 — connect_table: engineering.bom** — add column plant_id (BIGINT) with FK to production.plant.plant_id — BOMs are plant-specific in manufacturing (plant-level BOM assignment) for production planning.
**PRIORITY 34 — connect_table: production.production_goods_receipt** — add column warehouse_id (BIGINT) with FK to inventory.warehouse.warehouse_id — goods receipts from production are received into a warehouse and this link is missing despite stock_location being present.
**PRIORITY 35 — connect_table: order.goods_issue** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — goods issues are frequently triggered by production work orders for component consumption.
**PRIORITY 36 — connect_table: asset.spare_part** — add column plant_id (BIGINT) with FK to production.plant.plant_id — spare parts are stocked and managed at specific plants for maintenance operations.
**PRIORITY 37 — rename_attribute: supply.demand_plan_version** — rename column superseded_by_version_demand_plan_version_id to superseded_by_version_id because the column name redundantly embeds the full table name.
**PRIORITY 38 — connect_table: quality.inspection_lot** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — inspection lots are created during production execution and must link to the work order for in-process quality control.
**PRIORITY 39 — connect_table: order.rma** — add column production_plant_id (BIGINT) with FK to production.plant.plant_id — RMAs need to identify which plant will process the return for repair or replacement.
**PRIORITY 40 — connect_table: logistics.delivery_note** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — delivery notes for make-to-order items should trace to the production work order that fulfilled them.
**PRIORITY 41 — connect_table: quality.supplier_quality_audit** — add column plant_id (BIGINT) with FK to production.plant.plant_id — supplier quality audits are conducted relative to specific receiving plants and their quality standards.
**PRIORITY 42 — connect_table: production.resource_tool** — add column calibration_record_id (BIGINT) with FK to asset.calibration_record.calibration_record_id — production tools and measuring resources require calibration traceability for quality compliance.

Other known issues from static analysis (7):
  - Table engineering.engineering_bom_line has 2 FK columns pointing to engineering.component (component_id, tertiary_engineering_substitute_component_id) (generic/unlabeled: component_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table product.order_line has 2 FK columns pointing to inventory.warehouse (warehouse_id, order_warehouse_id) (generic/unlabeled: warehouse_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.delivery has 2 FK columns pointing to customer.customer_account (customer_account_id, delivery_ship_to_party_customer_account_id) (generic/unlabeled: customer_account_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Attribute product.order_line.notes has unrecognized data type 'TEXT' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Product 'production.production_work_order' has 26 outgoing FKs (max recommended: 25). Consider splitting into multiple products or refactoring as a junction.
  - 48 attribute(s) match person-data patterns but lack pii_ tags
  - 2 attribute(s) have pipe-enum value_regex with >8 values — consider creating a reference product instead

Deterministic score: 98.58/100 (LLM assessment: 62/100)
