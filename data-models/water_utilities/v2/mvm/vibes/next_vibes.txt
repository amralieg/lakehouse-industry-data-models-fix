**Model Quality Score: 79.64/100**

**PRIORITY 1 — rename_product: compliance.regulatory_agency** — the SELF-REF FK is listed twice with an invalid raw form `parent_regulatory_agency_regulatory_agency_id`; keep only `parent_regulatory_agency_id` and remove the duplicate self-ref entry because the doubled column violates the FK naming convention.
**PRIORITY 2 — rename_attribute: billing.rate_schedule** — rename column superseded_by_rate_schedule_billing_rate_schedule_id to superseded_by_rate_schedule_id because the current name contains a redundant embedded table-name segment violating the foreign_key_suffix convention.
**PRIORITY 3 — connect_table: metering.meter** — add column installation_id (BIGINT) with FK to metering.installation.installation_id — the meter is the central metering asset yet has zero outbound FKs; linking it to its installation ties it into the domain graph and to service context.
**PRIORITY 4 — connect_table: treatment.chemical** — add column facility_id (BIGINT) with FK to treatment.facility.facility_id — chemical is an orphan-risk reference table with no outbound link; associate each chemical inventory record with its owning facility for dosing traceability.
**PRIORITY 5 — connect_table: treatment.water_source** — add column facility_id (BIGINT) with FK to treatment.facility.facility_id — water_source has zero outbound FKs; linking source to the treating facility completes the source-to-treatment lineage.
**PRIORITY 6 — connect_table: distribution.flow_reading** — add column ami_endpoint_id (BIGINT) with FK to metering.ami_endpoint.ami_endpoint_id — flow readings frequently originate from AMI endpoints; add explicit device provenance beyond meter reference.
**PRIORITY 7 — connect_table: metering.read** — add column meter_id (BIGINT) with FK to metering.meter.meter_id — read currently links only to installation/endpoint/route; direct meter linkage supports reads captured against a swapped meter within an installation.
**PRIORITY 8 — connect_table: wastewater.manhole** — add column sewer_network_id (BIGINT) with FK to wastewater.sewer_network.sewer_network_id — manhole only links to asset.registry and is otherwise isolated within wastewater topology; tie each manhole to its sewer network.
**PRIORITY 9 — connect_table: distribution.main_break** — add column work_order_id (BIGINT) with FK to asset.work_order.work_order_id — main breaks trigger corrective maintenance; link the break event to the work order that resolves it.
**PRIORITY 10 — connect_table: distribution.leak_detection_survey** — add column work_order_id (BIGINT) with FK to asset.work_order.work_order_id — leak survey findings drive repair work orders; add the linkage for closure tracking.
**PRIORITY 11 — connect_table: quality.analytical_result** — add column dmr_result_id (BIGINT) with FK to compliance.dmr_result.dmr_result_id — analytical results feed DMR results; the reverse link exists but analytical_result should reference its reporting context. (Alternatively verify direction; if dmr_result already owns it, skip.)
**PRIORITY 12 — connect_table: treatment.finished_water_production** — add column process_unit_id (BIGINT) with FK to treatment.process_unit.process_unit_id — production output originates from specific process units; only facility is currently linked, losing per-unit attribution.
**PRIORITY 13 — connect_table: wastewater.effluent_parameter_result** — add column analytical_result_id (BIGINT) with FK to quality.analytical_result.analytical_result_id — effluent parameter measurements are analytical results; link to the quality domain result record to avoid duplicated measurement data.
**PRIORITY 14 — connect_table: metering.accuracy_test** — add column work_order_id (BIGINT) with FK to asset.work_order.work_order_id — accuracy tests are scheduled maintenance activities; link to the work order that authorized the test.
**PRIORITY 15 — connect_table: distribution.hydrant** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — network_valve links to asset.registry but hydrant does not, creating inconsistent asset-registry coverage for physical distribution components.
**PRIORITY 16 — connect_table: distribution.pipe_main** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — pipe_main is a major physical asset lacking a link to the asset registry, breaking asset lifecycle traceability.
**PRIORITY 17 — connect_table: distribution.storage_tank** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — storage_tank links to asset.location but not asset.registry, leaving it outside the asset lifecycle inventory.
**PRIORITY 18 — connect_table: distribution.pump_station** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — pump_station links to asset.location but not asset.registry, inconsistent with other physical asset registry coverage.
**PRIORITY 19 — connect_table: treatment.process_unit** — add column asset_registry_id (BIGINT) with FK to asset.registry.registry_id — process_unit is a maintainable physical asset but links only to location; register it for maintenance and condition tracking.
**PRIORITY 20 — connect_table: quality.contaminant** — add column contaminant_limit_id (BIGINT) with FK to quality.contaminant_limit.contaminant_limit_id — verify direction; contaminant references sampling_point but not its governing regulatory limit directly, whereas MCL association is core to quality tracking. (If limit references contaminant, skip.)

Other known issues from static analysis (4):
  - Table distribution.hydrant has 2 FK columns pointing to distribution.pipe_main (pipe_main_id, hydrant_pipe_main_id) (generic/unlabeled: pipe_main_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table billing.payment has 2 FK columns pointing to billing.invoice (invoice_id, payment_invoice_id) (generic/unlabeled: invoice_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 76 attribute(s) match person-data patterns but lack pii_ tags
  - PK columns use 2 different datatypes: BIGINT, DECIMAL(10,4). Recommend all PKs use BIGINT.

Deterministic score: 79.64/100 (LLM assessment: 88/100)
