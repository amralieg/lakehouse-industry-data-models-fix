**Model Quality Score: 85.98/100**

**PRIORITY 1 — connect_table: foodsafety.illness_report** — add column guest_profile_id (BIGINT) with FK to guest.profile.profile_id — illness reports should link to the guest profile experiencing the illness, not only loyalty.member which excludes non-member guests.
**PRIORITY 2 — connect_table: guest.address** — add column address_type_id or remove ambiguity by connecting to order.delivery_order — currently guest.address links to guest.profile and restaurant.unit but delivery_order/catering_order reference guest.address; verify bidirectional integrity by adding FK from guest.address to a canonical owner.
**PRIORITY 3 — connect_table: menu.combo_meal** — add column recipe_id (BIGINT) with FK to menu.recipe.recipe_id — combo meals aggregate items with recipes and need traceability for costing/nutrition rollups.
**PRIORITY 4 — connect_table: menu.item_listing** — add column channel_id (BIGINT) with FK to order.channel.channel_id — item listings vary by channel (DT/OLO/3PD) and must be scoped to channel for PMIX and pricing.
**PRIORITY 5 — connect_table: menu.item_listing** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — listings differ per unit; without unit scope this table cannot support per-location menu availability.
**PRIORITY 6 — connect_table: restaurant.operating_hours** — add column daypart_id or channel_id (BIGINT) with FK to order.channel.channel_id — operating hours differ by channel (dine-in vs drive-thru vs delivery) and need channel scope.
**PRIORITY 7 — connect_table: workforce.department** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — departments must be scoped to a restaurant unit or brand for org hierarchy; currently only self-references.
**PRIORITY 8 — connect_table: workforce.department** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — enables cross-unit departments rolled to a brand.
**PRIORITY 9 — connect_table: loyalty.program** — add column owner_employee_id (BIGINT) with FK to workforce.employee.employee_id — programs need a responsible owner for governance and audit.
**PRIORITY 10 — connect_table: loyalty.tier** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — tiers are frequently brand-scoped for multi-brand loyalty operators.
**PRIORITY 11 — connect_table: restaurant.brand** — add column headquarters_address_id (BIGINT) with FK to guest.address.address_id — brands need a canonical address linkage or drop guest.address's restaurant.unit FK for consistency.
**PRIORITY 12 — connect_table: supply.supplier** — add column address_id (BIGINT) with FK to guest.address.address_id — suppliers must have an address for logistics and receiving.
**PRIORITY 13 — connect_table: supply.ingredient** — add column allergen_declaration_id (BIGINT) with FK to menu.allergen_declaration.allergen_declaration_id — ingredients need allergen linkage for compliance rollups; currently declarations link to ingredient one-way only in weak form.
**PRIORITY 14 — connect_table: order.tax** — add column pos_terminal_id (BIGINT) with FK to restaurant.pos_terminal.pos_terminal_id — tax records are captured at terminal and need terminal traceability for audit.
**PRIORITY 15 — connect_table: order.channel** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — channels are often enabled per unit; without a unit link channel availability cannot be modeled.
**PRIORITY 16 — connect_table: foodsafety.critical_control_point** — add column recipe_id (BIGINT) with FK to menu.recipe.recipe_id — CCPs frequently apply at recipe steps and need direct recipe traceability beyond haccp_plan.
**PRIORITY 17 — connect_table: menu.modifier_group** — add column menu_item_id (BIGINT) with FK to menu.menu_item.menu_item_id — modifier groups attach to items, not only to menus, for proper cardinality.
**PRIORITY 18 — connect_table: menu.nutrition_profile** — add column allergen_declaration_id (BIGINT) with FK to menu.allergen_declaration.allergen_declaration_id — nutrition and allergens are jointly governed and should be linked.
**PRIORITY 19 — connect_table: inventory.uom** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — UoM standards can be brand-scoped for multi-brand operators; otherwise uom is only weakly integrated cross-domain.
**PRIORITY 20 — connect_table: workforce.position** — add column department_id (BIGINT) with FK to workforce.department.department_id — positions belong to departments; currently no department linkage exists.
**PRIORITY 21 — connect_table: workforce.certification** — add column department_id (BIGINT) with FK to workforce.department.department_id — certifications are frequently required per department (BOH vs FOH).
**PRIORITY 22 — connect_table: restaurant.equipment_asset** — add column supplier_id (BIGINT) with FK to supply.supplier.supplier_id — equipment procurement/service vendor relationships are essential for asset management.
**PRIORITY 23 — connect_table: restaurant.equipment_asset** — add column stock_location_id (BIGINT) with FK to inventory.stock_location.stock_location_id — equipment resides in storage/kitchen locations for maintenance and food safety context.
**PRIORITY 24 — connect_table: guest.preference** — add column loyalty_member_id (BIGINT) with FK to loyalty.member.member_id — preferences should be linkable to loyalty member for personalization campaigns.
**PRIORITY 25 — connect_table: guest.consent_record** — add column channel_id (BIGINT) with FK to order.channel.channel_id — consent is captured per channel (OLO web, app, POS) and must be channel-scoped for privacy compliance.
**PRIORITY 26 — connect_table: loyalty.member_offer_assignment** — add column program_id (BIGINT) with FK to loyalty.program.program_id — assignments need program scope for multi-program operators.
**PRIORITY 27 — connect_table: loyalty.accrual_rule** — add column reward_id (BIGINT) with FK to loyalty.reward.reward_id — accrual rules often target specific rewards.
**PRIORITY 28 — connect_table: order.kds_ticket** — add column pos_terminal_id (BIGINT) with FK to restaurant.pos_terminal.pos_terminal_id — KDS tickets originate from POS terminals; needed for source traceability.
**PRIORITY 29 — connect_table: supply.purchase_order_line** — add column uom_id (BIGINT) with FK to inventory.uom.uom_id — PO lines require unit of measure for quantity semantics.
**PRIORITY 30 — connect_table: supply.purchase_order_line** — add column contract_price_id (BIGINT) with FK to supply.contract_price.contract_price_id — PO lines should trace to the contract price used for spend/variance analytics.
**PRIORITY 31 — connect_table: supply.goods_receipt_line** — add column purchase_order_id (BIGINT) with FK to supply.purchase_order.purchase_order_id — receipt lines currently only link to PO line; header-level PO link simplifies reconciliation.
**PRIORITY 32 — connect_table: foodsafety.corrective_action** — add column workforce_shift_id (BIGINT) with FK to workforce.shift.shift_id — corrective actions occur within a shift context for accountability.
**PRIORITY 33 — connect_table: foodsafety.audit_ccp_finding** — add column corrective_action_id (BIGINT) with FK to foodsafety.corrective_action.corrective_action_id — findings drive corrective actions; direct link required.
**PRIORITY 34 — connect_table: foodsafety.sanitation_schedule** — add column shift_id (BIGINT) with FK to workforce.shift.shift_id — sanitation tasks are executed during shifts and need shift traceability.
**PRIORITY 35 — connect_table: inventory.stock_transfer** — add column receiving_employee_id (BIGINT) with FK to workforce.employee.employee_id — transfers need both sender and receiver; add receiving employee alongside existing employee link, and rename existing employee_id to sending_employee_id.
**PRIORITY 36 — rename_attribute: inventory.stock_transfer** — rename column employee_id to sending_employee_id
**PRIORITY 37 — connect_table: guest.visit** — add column guest_order_id (BIGINT) with FK to order.guest_order.guest_order_id — visits should link to the order created during the visit for revenue attribution.
**PRIORITY 38 — connect_table: menu.pmix_record** — add column shift_id (BIGINT) with FK to workforce.shift.shift_id — PMIX is analyzed by daypart/shift and benefits from shift linkage.
**PRIORITY 39 — connect_table: order.discount** — add column accrual_rule_id or program_id (BIGINT) with FK to loyalty.program.program_id — discounts derived from loyalty programs need program traceability.
**PRIORITY 40 — connect_table: workforce.payroll_record** — add column position_id (BIGINT) with FK to workforce.position.position_id — payroll rates are position-dependent; direct link needed for audit.
**PRIORITY 41 — connect_table: restaurant.format_config** — add column location_profile_id (BIGINT) with FK to restaurant.location_profile.location_profile_id — format configuration varies by location profile characteristics.
**PRIORITY 42 — connect_table: supply.ingredient_lot** — add column purchase_order_line_id (BIGINT) with FK to supply.purchase_order_line.purchase_order_line_id — lot traceability back to the PO line is required for recall management.
**PRIORITY 43 — connect_table: foodsafety.health_inspection** — add column corrective_action_id (BIGINT) with FK to foodsafety.corrective_action.corrective_action_id — inspections trigger corrective actions and should link directly.
**PRIORITY 44 — connect_table: foodsafety.temperature_log** — add column ingredient_lot_id (BIGINT) with FK to supply.ingredient_lot.ingredient_lot_id — temperature excursions on specific lots are core to cold-chain traceability.
**PRIORITY 45 — connect_table: menu.item_price** — add column channel_id (BIGINT) with FK to order.channel.channel_id — pricing differs by channel (3PD markup, OLO discount); channel scope is essential.
**PRIORITY 46 — connect_table: guest.satisfaction_survey** — add column complaint_id (BIGINT) with FK to guest.complaint.complaint_id — surveys often surface complaints; direct link supports CSAT/complaint correlation.
**PRIORITY 47 — connect_table: order.payment** — add column tax_id (BIGINT) with FK to order.tax.tax_id — payments should reference tax records for reconciliation.
**PRIORITY 48 — connect_table: loyalty.reward** — add column tier_id (BIGINT) with FK to loyalty.tier.tier_id — rewards are typically tier-gated and need tier eligibility linkage.

Other known issues from static analysis (32):
  - Table menu.recipe_ingredient has 2 FK columns pointing to supply.ingredient (ingredient_id, recipe_substitute_ingredient_id) (generic/unlabeled: ingredient_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.profile has 2 FK columns pointing to restaurant.unit (unit_id, profile_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.address has 3 FK columns pointing to guest.profile (profile_id, address_profile_id, owner_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.preference has 2 FK columns pointing to guest.profile (profile_id, preference_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.satisfaction_survey has 2 FK columns pointing to restaurant.unit (unit_id, tertiary_satisfaction_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.complaint has 2 FK columns pointing to guest.profile (profile_id, complaint_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.complaint has 2 FK columns pointing to restaurant.unit (unit_id, complaint_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.interaction has 2 FK columns pointing to guest.profile (profile_id, interaction_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.interaction has 2 FK columns pointing to restaurant.unit (unit_id, interaction_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.order_modifier has 2 FK columns pointing to menu.menu_modifier (menu_modifier_id, source_modifier_menu_modifier_id) (generic/unlabeled: menu_modifier_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.refund has 3 FK columns pointing to workforce.employee (employee_id, primary_refund_employee_id, tertiary_refund_voided_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.refund has 2 FK columns pointing to guest.profile (profile_id, refund_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supply.goods_receipt has 2 FK columns pointing to workforce.employee (employee_id, primary_goods_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supply.goods_receipt_line has 2 FK columns pointing to workforce.employee (employee_id, receiving_user_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.receiving_order has 2 FK columns pointing to workforce.employee (employee_id, receiving_manager_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to restaurant.unit (unit_id, origin_restaurant_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to inventory.stock_location (stock_location_id, origin_stock_location_id) (generic/unlabeled: stock_location_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to workforce.employee (employee_id, tertiary_stock_received_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.employee has 3 FK columns pointing to restaurant.unit (unit_id, employee_unit_id, employee_work_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.shift has 2 FK columns pointing to workforce.employee (employee_id, primary_shift_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.schedule has 2 FK columns pointing to workforce.employee (employee_id, schedule_manager_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table foodsafety.food_safety_audit has 2 FK columns pointing to workforce.employee (employee_id, primary_food_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table foodsafety.corrective_action has 3 FK columns pointing to workforce.employee (employee_id, corrective_foodsafety_verified_by_employee_id, primary_foodsafety_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table foodsafety.corrective_action has 2 FK columns pointing to foodsafety.food_safety_audit (food_safety_audit_id, corrective_foodsafety_related_food_safety_audit_id) (generic/unlabeled: food_safety_audit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table foodsafety.corrective_action has 2 FK columns pointing to foodsafety.health_inspection (health_inspection_id, corrective_foodsafety_related_inspection_health_inspection_id) (generic/unlabeled: health_inspection_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 2 FK columns pointing to guest.profile (profile_id, member_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 3 FK columns pointing to restaurant.unit (unit_id, member_unit_id, primary_member_preferred_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.points_ledger has 2 FK columns pointing to order.guest_order (guest_order_id, source_transaction_guest_order_id) (generic/unlabeled: guest_order_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.redemption has 2 FK columns pointing to restaurant.unit (unit_id, redemption_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Attribute supply.supplier.address has unrecognized data type 'TEXT' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - 77 attribute(s) match person-data patterns but lack pii_ tags
  - 14 non-PK attributes redundantly prefixed with their product name

Deterministic score: 85.98/100 (LLM assessment: 78/100)
