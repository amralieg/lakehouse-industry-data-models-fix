**Model Quality Score: 74.95/100**

**Static Analysis Findings (2 actionable):**
  - [SA:unlinked_fk] Column provider.specialty.clinician_id looks like an FK but has no foreign_key_to reference
  - [SA:denormalized_natural_key] Product 'insurance.health_plan' has both FK 'group_id' and natural key 'group_number' for entity 'group'. The natural key is redundant — use the FK join instead.

**PRIORITY 1 — connect_table: insurance.payer** — add column primary_health_plan_id (BIGINT) with FK to insurance.health_plan.health_plan_id — payer must reference its flagship health plan for benefit administration lookups.
**PRIORITY 2 — rename_attribute: insurance.provider_network** — rename column parent_provider_network_id to parent_network_id
**PRIORITY 3 — remove_fk: insurance.provider_network** — remove FK on column parent_provider_network_id because it duplicates parent_network_provider_network_id which already provides the self-ref hierarchy.
**PRIORITY 4 — rename_attribute: insurance.payer** — rename column primary_successor_payer_id to successor_payer_id
**PRIORITY 5 — rename_attribute: insurance.coverage_policy** — rename column primary_superseded_by_coverage_policy_id to superseded_by_coverage_policy_id
**PRIORITY 6 — rename_attribute: insurance.dependent** — rename column primary_dependent_id to parent_dependent_id
**PRIORITY 7 — rename_attribute: insurance.fee_schedule** — rename column primary_predecessor_schedule_fee_schedule_id to predecessor_fee_schedule_id
**PRIORITY 8 — rename_attribute: insurance.fee_schedule_line** — rename column primary_superseded_by_fee_schedule_line_id to superseded_by_fee_schedule_line_id
**PRIORITY 9 — rename_attribute: radiology.protocol** — rename column primary_superseded_by_protocol_id to superseded_by_protocol_id
**PRIORITY 10 — rename_attribute: encounter.discharge_summary** — rename column addended_discharge_summary_id to addended_by_discharge_summary_id
**PRIORITY 11 — rename_attribute: clinical.advance_directive** — rename column superseded_by_directive_advance_directive_id to superseded_by_advance_directive_id
**PRIORITY 12 — rename_attribute: insurance.provider_network** — rename column parent_network_provider_network_id to parent_network_id
**PRIORITY 13 — connect_table: provider.specialty** — add column parent_specialty_id (BIGINT) with FK to provider.specialty.specialty_id — specialties have taxonomies (e.g., sub-specialties under parent boards) that should be modeled hierarchically.
**PRIORITY 14 — connect_table: patient.address** — add column mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — address should link directly to the master patient identity rather than only through demographics.
**PRIORITY 15 — connect_table: patient.emergency_contact** — add column demographics_id (BIGINT) with FK to patient.demographics.demographics_id — emergency contacts are typically captured as part of demographic registration and need that linkage.
**PRIORITY 16 — connect_table: patient.guarantor** — add column demographics_id (BIGINT) with FK to patient.demographics.demographics_id — guarantor identity ties to demographic data for name/address resolution.
**PRIORITY 17 — connect_table: laboratory.reference_range** — add column test_result_id (BIGINT) with FK to laboratory.test_result.test_result_id — reference ranges applied to specific results need traceability back to results that used them.
**PRIORITY 18 — connect_table: billing.statement** — add column guarantor_id (BIGINT) with FK to patient.guarantor.guarantor_id — statements are sent to guarantors, not just patient accounts.
**PRIORITY 19 — connect_table: billing.statement** — add column mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — statements must resolve to the patient identity for statement history reporting.
**PRIORITY 20 — connect_table: billing.payment** — add column patient_account_id (BIGINT) with FK to billing.patient_account.patient_account_id — payments should link directly to the patient account, not just invoice.
**PRIORITY 21 — connect_table: claim.eligibility** — add column patient_insurance_coverage_id (BIGINT) with FK to patient.insurance_coverage.insurance_coverage_id — eligibility checks are performed against a specific coverage record.
**PRIORITY 22 — connect_table: claim.remittance** — add column payment_id (BIGINT) with FK to billing.payment.payment_id — remittance advice generates payment postings and needs an operational link.
**PRIORITY 23 — connect_table: provider.clinician** — add column primary_org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — clinicians need a primary organizational affiliation for reporting and directory lookups.
**PRIORITY 24 — connect_table: provider.clinician** — add column dea_registration_id (BIGINT) with FK to provider.dea_registration.dea_registration_id — clinicians need a direct link to their current DEA registration for prescribing authority checks.
**PRIORITY 25 — connect_table: pharmacy.drug_master** — add column formulary_id (BIGINT) with FK to pharmacy.formulary.formulary_id — drugs need direct formulary participation lookup rather than reverse-only navigation.
**PRIORITY 26 — remove_fk: pharmacy.drug_master** — remove FK on column test_catalog_id because drug master referencing a lab test catalog has no business meaning; drugs and lab tests are distinct catalogs.
**PRIORITY 27 — remove_fk: radiology.protocol** — remove FK on column test_catalog_id because a radiology protocol referencing a laboratory test catalog is not a valid business relationship.
**PRIORITY 28 — remove_fk: clinical.allergy** — remove FK on column test_result_id because allergy records do not directly reference a single lab result; allergy findings sourced from labs should be linked via observation.
**PRIORITY 29 — connect_table: clinical.vital_sign** — add column clinical_order_id (BIGINT) with FK to order.clinical_order.clinical_order_id — vital sign captures often result from a specific clinical order for measurement.
**PRIORITY 30 — connect_table: clinical.problem** — add column patient_demographics_id (BIGINT) with FK to patient.demographics.demographics_id — problem list entries need patient demographic linkage consistent with diagnosis/allergy.
**PRIORITY 31 — connect_table: encounter.visit** — add column patient_demographics_id (BIGINT) with FK to patient.demographics.demographics_id — visits should carry the demographic snapshot pointer along with mpi_record.
**PRIORITY 32 — connect_table: encounter.visit** — add column registration_event_id (BIGINT) with FK to patient.registration_event.registration_event_id — visits are produced by registration events and require the linkage.
**PRIORITY 33 — connect_table: encounter.bed_assignment** — add column schedulable_resource_id (BIGINT) with FK to scheduling.schedulable_resource.schedulable_resource_id — beds are schedulable resources and should be tied to the resource inventory.
**PRIORITY 34 — connect_table: scheduling.surgical_case** — add column visit_procedure_id (BIGINT) with FK to encounter.visit_procedure.visit_procedure_id — surgical cases materialize as visit procedures and require the operational link.
**PRIORITY 35 — connect_table: scheduling.surgical_case** — add column appointment_id (BIGINT) with FK to radiology.appointment.appointment_id — surgical scheduling often shares appointment infrastructure; if not, use scheduling appointment; missing appointment tie leaves surgical case disconnected from scheduling appointment stream.
**PRIORITY 36 — connect_table: scheduling.telehealth_session** — add column schedulable_resource_id (BIGINT) with FK to scheduling.schedulable_resource.schedulable_resource_id — telehealth uses virtual room resources that need tracking.
**PRIORITY 37 — connect_table: order.routing** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — order routing targets an organizational destination and needs that FK to be operational.
**PRIORITY 38 — connect_table: order.routing** — add column clinician_id (BIGINT) with FK to provider.clinician.clinician_id — order routing can target an individual clinician.
**PRIORITY 39 — connect_table: order.set** — add column specialty_id (BIGINT) with FK to provider.specialty.specialty_id — order sets are already partially specialty-scoped; ensure the linkage exists (verify not duplicate).
**PRIORITY 40 — connect_table: pharmacy.formulary** — add column payer_id (BIGINT) with FK to insurance.payer.payer_id — formularies are typically owned by a payer and need direct payer attribution.
**PRIORITY 41 — connect_table: pharmacy.inventory** — add column schedulable_resource_id (BIGINT) with FK to scheduling.schedulable_resource.schedulable_resource_id — pharmacy inventory locations are physical resources; if not modeled, remove — otherwise link.
**PRIORITY 42 — connect_table: radiology.modality** — add column protocol_id (BIGINT) with FK to radiology.protocol.protocol_id — modalities are associated with default protocols for scheduling.
**PRIORITY 43 — connect_table: radiology.report** — add column dicom_series_id (BIGINT) with FK to radiology.dicom_series.dicom_series_id — radiology reports reference the DICOM series they interpret.
**PRIORITY 44 — connect_table: clinical.care_plan** — add column care_team_id (BIGINT) with FK to clinical.care_team.care_team_id — care plans are executed by care teams; direct link needed.
**PRIORITY 45 — connect_table: clinical.diagnosis** — add column encounter_visit_id (BIGINT) with FK to encounter.visit.visit_id — diagnoses are captured during visits and need a direct FK to the visit.
**PRIORITY 46 — connect_table: insurance.benefit** — add column payer_id (BIGINT) with FK to insurance.payer.payer_id — benefits are ultimately owned by a payer.
**PRIORITY 47 — connect_table: insurance.plan_network** — add column payer_id (BIGINT) with FK to insurance.payer.payer_id — plan-network relationships operationally roll up to a payer.
**PRIORITY 48 — connect_table: claim.diagnosis_link** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — diagnosis linkage on claims should trace back to the source visit.
**PRIORITY 49 — connect_table: patient.portal_account** — add column emergency_contact_id (BIGINT) with FK to patient.emergency_contact.emergency_contact_id — proxy/family portal accounts often link to emergency contact records for identity resolution.
**PRIORITY 50 — connect_table: patient.pcp_attribution** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — PCP attribution is also to an organization (medical group); ensure both individual and org attribution paths exist.
**PRIORITY 51 — remove_fk: clinical.diagnosis** — remove FK on column health_plan_id because a raw diagnosis observation is not owned by a health plan; plan linkage belongs on claim or coverage, not the clinical fact.
**PRIORITY 52 — remove_fk: clinical.diagnosis** — remove FK on column payer_id because payer is not an attribute of the clinical diagnosis fact itself.
**PRIORITY 53 — remove_fk: clinical.procedure_event** — remove FK on column health_plan_id because health plan is not a property of a clinical procedure event; belongs on billing/claim artifacts.
**PRIORITY 54 — remove_fk: clinical.procedure_event** — remove FK on column payer_id because payer is not a property of a clinical procedure event.
**PRIORITY 55 — remove_fk: clinical.procedure_event** — remove FK on column pharmacy_drug_master_id because a procedure event should reference administered drugs via mar_record or pharmacy events, not directly to drug_master.
**PRIORITY 56 — remove_fk: encounter.visit_procedure** — remove FK on column prior_auth_rule_id because prior-auth rule linkage belongs to prior_authorization or claim.line, not the visit procedure fact.
**PRIORITY 57 — remove_fk: billing.charge** — remove FK on column prior_auth_rule_id because prior-auth rules should link to prior_authorization records, not directly to charges.
**PRIORITY 58 — remove_fk: order.diet_order** — remove FK on column coverage_policy_id because diet orders are not adjudicated against coverage policies.
**PRIORITY 59 — remove_fk: scheduling.waitlist_entry** — remove FK on column payer_id because payer belongs on the coverage/eligibility check, not directly on waitlist entries.
**PRIORITY 60 — remove_fk: pharmacy.dispense_event** — remove FK on column plan_network_id because dispense events are already linked to health_plan and member_enrollment; plan_network is not directly relevant at dispense time.
**PRIORITY 61 — connect_table: laboratory.test_catalog** — add column specialty_id (BIGINT) with FK to provider.specialty.specialty_id — test catalog entries are often specialty-scoped (pathology, microbiology) and need explicit linkage.
**PRIORITY 62 — connect_table: encounter.triage_assessment** — add column adt_event_id (BIGINT) with FK to encounter.adt_event.adt_event_id — triage occurs at ED registration ADT event.
**PRIORITY 63 — connect_table: pharmacy.adverse_drug_event** — add column allergy_id (BIGINT) with FK to clinical.allergy.allergy_id — ADEs may correlate to known allergy records for signal analysis.
**PRIORITY 64 — connect_table: pharmacy.mar_record** — add column clinical_order_id (BIGINT) with FK to order.clinical_order.clinical_order_id — MAR entries fulfill medication orders; direct link is required.
**PRIORITY 65 — connect_table: radiology.imaging_order** — add column modality_id (BIGINT) with FK to radiology.modality.modality_id — imaging orders specify the modality.
**PRIORITY 66 — connect_table: radiology.critical_result** — add column pathology_report_id (BIGINT) with FK to laboratory.pathology_report.pathology_report_id — critical findings can originate from pathology as well; if scoped to imaging only, ignore — otherwise link.
**PRIORITY 67 — connect_table: provider.credential** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — credentialing is often verified/held at the organizational provider level.
**PRIORITY 68 — connect_table: provider.privileging** — add column visit_procedure_id (BIGINT) with FK to encounter.visit_procedure.visit_procedure_id — remove if operational rollup already exists; otherwise privileging governs procedures performed.
**PRIORITY 69 — connect_table: claim.submission** — add column payer_contract_id (BIGINT) with FK to insurance.payer_contract.payer_contract_id — submissions are governed by a payer contract.
**PRIORITY 70 — connect_table: billing.adjustment** — add column payer_contract_id (BIGINT) with FK to insurance.payer_contract.payer_contract_id — contractual adjustments derive from payer contracts and need traceability.
**PRIORITY 71 — connect_table: billing.cdm_entry** — add column specialty_id (BIGINT) with FK to provider.specialty.specialty_id — CDM entries can be specialty-tagged for pricing rules.
**PRIORITY 72 — connect_table: insurance.subscriber** — add column demographics_id (BIGINT) with FK to patient.demographics.demographics_id — subscriber demographic details tie to patient demographics.
**PRIORITY 73 — connect_table: scheduling.open_slot** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — open slots belong to a site/organization.
**PRIORITY 74 — connect_table: scheduling.or_block** — add column surgical_case_id (BIGINT) with FK to scheduling.surgical_case.surgical_case_id — OR blocks are consumed by surgical cases; if bidirectional, ensure one direction is explicit.
**PRIORITY 75 — connect_table: order.fulfillment** — add column mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — fulfillment records need patient identity linkage for auditability.
**PRIORITY 76 — connect_table: order.set_item** — add column standing_order_id (BIGINT) with FK to order.standing_order.standing_order_id — set items may be used in standing orders; verify or link.
**PRIORITY 77 — connect_table: clinical.note** — add column encounter_adt_event_id (BIGINT) with FK to encounter.adt_event.adt_event_id — remove if not applicable; some notes (e.g., transfer summaries) directly correspond to ADT events.
**PRIORITY 78 — connect_table: pharmacy.controlled_substance_log** — add column adverse_drug_event_id (BIGINT) with FK to pharmacy.adverse_drug_event.adverse_drug_event_id — controlled substance events feed ADE monitoring when diversion or reactions occur.
**PRIORITY 79 — connect_table: encounter.visit_insurance** — add column member_enrollment_id (BIGINT) with FK to insurance.member_enrollment.member_enrollment_id — visit insurance coverage should identify the specific enrollment.
**PRIORITY 80 — connect_table: laboratory.blood_bank_unit** — add column mar_record_id (BIGINT) with FK to pharmacy.mar_record.mar_record_id — blood administration is recorded on the MAR; direct linkage aids traceability.

Other known issues from static analysis (29):
  - Attribute 'formulary_tier' in pharmacy.formulary has redundant product prefix 'formulary_'. Rename to 'tier'
  - Table patient.registration_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_registration_pcp_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_visit_discharging_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit_provider has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_visit_supervising_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.discharge_summary has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_discharge_follow_up_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table provider.privileging has 2 FK columns pointing to provider.clinician (clinician_id, privileging_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.procedure_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_procedure_anesthesia_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.note has 3 FK columns pointing to provider.clinician (clinician_id, primary_note_attending_provider_clinician_id, tertiary_note_cosigner_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.problem has 3 FK columns pointing to provider.clinician (clinician_id, problem_clinician_id, tertiary_problem_last_updated_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.allergy has 2 FK columns pointing to provider.clinician (clinician_id, primary_allergy_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.immunization has 2 FK columns pointing to provider.clinician (clinician_id, primary_care_physician_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.care_plan has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_care_pcp_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.clinical_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_clinical_authorizing_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.referral_order has 2 FK columns pointing to provider.clinician (clinician_id, receiving_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to patient.mpi_record (mpi_record_id, primary_prescription_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to provider.clinician (clinician_id, primary_prescription_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.lab_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_lab_cancelled_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.test_result has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_test_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.report has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_report_reading_radiologist_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.appointment has 3 FK columns pointing to provider.clinician (clinician_id, primary_radiology_clinician_id, tertiary_radiology_referring_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.critical_result has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_critical_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table claim.eligibility has 2 FK columns pointing to patient.mpi_record (mpi_record_id, eligibility_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table claim.cob has 2 FK columns pointing to insurance.payer (payer_id, tertiary_payer_id) (generic/unlabeled: payer_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table insurance.dependent has 2 FK columns pointing to patient.mpi_record (mpi_record_id, dependent_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Subdomain 'care_coordination' appears in 2 domains: clinical, patient. Consider renaming to qualified form.
  - Subdomain 'clinical_documentation' appears in 2 domains: clinical, encounter. Consider renaming to qualified form.
  - Subdomain 'order_management' appears in 3 domains: laboratory, order, radiology. Consider renaming to qualified form.
  - 7 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 7 attribute(s) match person-data patterns but lack pii_ tags

Deterministic score: 74.95/100 (LLM assessment: 72/100)
