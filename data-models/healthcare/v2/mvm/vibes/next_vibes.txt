**Model Quality Score: 84/100**

**Static Analysis Findings (1 actionable):**
  - [SA:denormalized_natural_key] Product 'pharmacy.rx_claim' has both FK 'claim_id' and natural key 'claim_number' for entity 'claim'. The natural key is redundant — use the FK join instead.

**PRIORITY 1 — rename_product: order.set** — rename to order_set because "set" is a SQL reserved word and will cause query failures without quoting
**PRIORITY 2 — rename_product: claim.line** — rename to claim_line because "line" is overly generic and conflicts with common SQL terminology; needs domain-qualified naming for clarity
**PRIORITY 3 — connect_table: encounter.visit** — add column encounter_visit_id (BIGINT) with FK to encounter.visit.visit_id on encounter.visit_diagnosis — visit_diagnosis has no FK to visit despite being a child entity that documents diagnoses recorded during a specific visit
**PRIORITY 4 — connect_table: encounter.visit_diagnosis** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — visit_diagnosis logically belongs to a visit but shows no FK to encounter.visit in its current links
**PRIORITY 5 — connect_table: encounter.drg_assignment** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — DRG assignments are always made in context of a specific inpatient visit/encounter
**PRIORITY 6 — connect_table: encounter.visit_procedure** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — visit_procedure is a child of visit but no FK to visit is listed
**PRIORITY 7 — connect_table: clinical.diagnosis** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — diagnoses are typically documented during a visit encounter and need linkage to the encounter domain
**PRIORITY 8 — connect_table: clinical.observation** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — clinical observations occur during encounters and need visit context for temporal anchoring
**PRIORITY 9 — connect_table: clinical.vital_sign** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — vital signs are captured during visits and require encounter context
**PRIORITY 10 — connect_table: clinical.procedure_event** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — procedures are performed during encounters and need direct visit linkage
**PRIORITY 11 — connect_table: clinical.allergy** — add column patient_mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — allergies are patient-level data requiring MPI linkage for longitudinal record
**PRIORITY 12 — connect_table: clinical.problem** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — problems are often identified/updated during visits and need encounter context
**PRIORITY 13 — connect_table: clinical.note** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — clinical notes are authored during encounters and require visit-level association
**PRIORITY 14 — connect_table: billing.coding_assignment** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — coding assignments are performed per encounter for billing purposes
**PRIORITY 15 — connect_table: claim.denial** — add column claim_id (BIGINT) with FK to claim.claim.claim_id — denials are always issued against a specific claim and must link directly
**PRIORITY 16 — connect_table: claim.remittance** — add column claim_id (BIGINT) with FK to claim.claim.claim_id — remittance advice is a payer response to a specific claim submission
**PRIORITY 17 — connect_table: laboratory.lab_order** — add column clinical_order_id (BIGINT) with FK to order.clinical_order.clinical_order_id — lab orders originate from CPOE clinical orders and need traceability
**PRIORITY 18 — connect_table: radiology.imaging_order** — add column clinical_order_id (BIGINT) with FK to order.clinical_order.clinical_order_id — imaging orders originate from CPOE and need linkage to the order domain
**PRIORITY 19 — connect_table: radiology.imaging_order** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — imaging orders are placed during encounters and need visit context
**PRIORITY 20 — connect_table: pharmacy.prescription** — add column clinical_order_id (BIGINT) with FK to order.clinical_order.clinical_order_id — prescriptions originate from CPOE medication orders
**PRIORITY 21 — connect_table: pharmacy.mar_record** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — medication administration occurs during inpatient encounters
**PRIORITY 22 — remove_fk: scheduling.scheduling_appointment** — remove FK on column dea_registration_id because a general scheduling appointment should not reference DEA registration; DEA is relevant only to controlled substance prescribing, not appointment booking
**PRIORITY 23 — remove_fk: billing.adjustment** — remove FK on column scheduling_appointment_id because adjustments are financial corrections tied to charges/invoices, not directly to appointments
**PRIORITY 24 — rename_attribute: claim.submission** — rename column original_submission_id to resubmitted_from_submission_id because the business relationship is a resubmission chain, not a generic "original" self-reference
**PRIORITY 25 — connect_table: radiology.study** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — imaging studies are performed during patient encounters and need visit-level context
**PRIORITY 26 — connect_table: radiology.report** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — radiology reports are generated in context of a patient encounter
**PRIORITY 27 — connect_table: laboratory.test_result** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — test results are associated with the encounter during which the specimen was collected
**PRIORITY 28 — connect_table: patient.address** — add column mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — addresses should link to the MPI for patient-level resolution, not only demographics
**PRIORITY 29 — remove_fk: billing.charge** — remove FK on column specialty_id because charges are tied to providers and CDM entries, not directly to specialties; the specialty is derivable through the clinician FK
**PRIORITY 30 — connect_table: order.fulfillment** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — order fulfillment occurs during encounters and needs visit context for operational tracking
**PRIORITY 31 — connect_table: clinical.immunization** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — immunizations are administered during visits and need encounter linkage
**PRIORITY 32 — connect_table: clinical.care_plan** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — care plans are often initiated or updated during specific encounters
**PRIORITY 33 — rename_attribute: radiology.protocol** — rename column primary_superseded_by_protocol_id to superseded_by_protocol_id because the "primary_" prefix is redundant and inconsistent with naming conventions elsewhere
**PRIORITY 34 — connect_table: provider.group_membership** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — group memberships exist within organizational contexts and need facility/org linkage
**PRIORITY 35 — connect_table: encounter.discharge_summary** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — discharge summaries are produced for specific inpatient visits
**PRIORITY 36 — connect_table: billing.payment** — add column clinician_id (BIGINT) with FK to provider.clinician.clinician_id — payments often need attribution to rendering provider for revenue allocation
**PRIORITY 37 — connect_table: claim.eligibility** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — eligibility checks are performed in context of a visit for real-time verification
**PRIORITY 38 — remove_fk: scheduling.scheduling_appointment** — remove FK on column problem_id because linking appointments directly to clinical problems bypasses proper order/referral workflows
**PRIORITY 39 — connect_table: pharmacy.inventory** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — pharmacy inventory is managed at the organizational/facility level
**PRIORITY 40 — connect_table: laboratory.blood_bank_unit** — add column clinician_id (BIGINT) with FK to provider.clinician.clinician_id — blood bank units require ordering/collecting clinician attribution for traceability
**PRIORITY 41 — rename_attribute: clinical.advance_directive** — rename column superseded_by_directive_advance_directive_id to superseded_by_advance_directive_id because "directive_advance_directive" is redundant stuttering
**PRIORITY 42 — connect_table: radiology.radiology_appointment** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — radiology appointments occur within the context of patient encounters
**PRIORITY 43 — connect_table: order.diet_order** — add column mpi_record_id (BIGINT) with FK to patient.mpi_record.mpi_record_id — diet orders are patient-level and need MPI linkage for longitudinal tracking
**PRIORITY 44 — connect_table: order.therapy_order** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — therapy orders are placed during encounters and need visit context
**PRIORITY 45 — connect_table: pharmacy.controlled_substance_log** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — controlled substance logs must track the facility where the event occurred for DEA compliance
**PRIORITY 46 — connect_table: clinical.care_team** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — care teams operate within organizational contexts
**PRIORITY 47 — remove_fk: radiology.critical_result** — remove FK on column drg_id because critical radiology results have no business relationship to DRG grouping; DRG is a billing/discharge concept
**PRIORITY 48 — connect_table: provider.board_certification** — add column org_provider_id (BIGINT) with FK to provider.org_provider.org_provider_id — board certifications are verified by organizations during credentialing
**PRIORITY 49 — connect_table: encounter.triage_assessment** — add column visit_id (BIGINT) with FK to encounter.visit.visit_id — triage assessments already show FK to visit but confirming the link exists; if missing, it must be added as triage occurs at visit intake
**PRIORITY 50 — rename_product: reference.crosswalk** — rename to code_crosswalk because "crosswalk" alone is ambiguous and does not convey it maps between code systems

Other known issues from static analysis (36):
  - Table patient.registration_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_registration_pcp_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table patient.patient_coverage has 2 FK columns pointing to patient.mpi_record (mpi_record_id, tertiary_patient_subscriber_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit has 3 FK columns pointing to provider.clinician (clinician_id, visit_clinician_id, visit_discharging_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit has 2 FK columns pointing to patient.mpi_record (mpi_record_id, visit_patient_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit_provider has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_visit_supervising_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.discharge_summary has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_discharge_follow_up_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table provider.privileging has 2 FK columns pointing to provider.clinician (clinician_id, privileging_proctor_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.procedure_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_procedure_anesthesia_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.note has 3 FK columns pointing to provider.clinician (clinician_id, note_clinician_id, note_cosigner_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.problem has 3 FK columns pointing to provider.clinician (clinician_id, problem_clinician_id, problem_last_updated_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.allergy has 2 FK columns pointing to provider.clinician (clinician_id, allergy_override_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.immunization has 3 FK columns pointing to provider.clinician (clinician_id, immunization_ordering_provider_clinician_id, primary_care_physician_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.care_plan has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_care_pcp_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.care_team has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_care_member_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.clinical_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_clinical_authorizing_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.referral_order has 2 FK columns pointing to provider.clinician (clinician_id, receiving_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.fulfillment has 2 FK columns pointing to provider.clinician (clinician_id, fulfillment_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to provider.clinician (clinician_id, prescription_prescriber_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to patient.mpi_record (mpi_record_id, prescription_patient_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.lab_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_lab_cancelled_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.test_result has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_test_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.report has 3 FK columns pointing to provider.clinician (clinician_id, report_clinician_id, report_reading_radiologist_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.radiology_appointment has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_radiology_referring_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.radiology_appointment has 2 FK columns pointing to scheduling.scheduling_appointment (scheduling_appointment_id, radiology_scheduling_appointment_id) (generic/unlabeled: scheduling_appointment_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table radiology.critical_result has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_critical_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table billing.charge has 2 FK columns pointing to provider.clinician (clinician_id, charge_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table billing.billing_coverage has 2 FK columns pointing to patient.mpi_record (mpi_record_id, billing_patient_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table claim.eligibility has 2 FK columns pointing to patient.mpi_record (mpi_record_id, eligibility_patient_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table reference.crosswalk has 2 FK columns pointing to reference.code_set_version (code_set_version_id, source_code_set_version_id) (generic/unlabeled: code_set_version_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - PII-candidate attribute pharmacy.prescription.prescription_patient_mpi_record_id has no classification tags — expected restricted/confidential + PII tag
  - PII-candidate attribute pharmacy.prescription.prescription_prescriber_clinician_id has no classification tags — expected restricted/confidential + PII tag
  - Subdomain 'clinical_documentation' appears in 2 domains: clinical, encounter. Consider renaming to qualified form.
  - Subdomain 'order_management' appears in 2 domains: order, radiology. Consider renaming to qualified form.
  - 3 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 76 attribute(s) match person-data patterns but lack pii_ tags
  - 17 non-PK attributes redundantly prefixed with their product name

Deterministic score: 84/100 (LLM assessment: 72/100)
