**Model Quality Score: 97.4/100**

**PRIORITY 1 — rename_attribute: store.sales_territory** — rename column parent_territory_id to parent_sales_territory_id because sales_territory already has a SELF-REF via parent_sales_territory_id and a duplicate redundant parent_territory_id self-ref column exists; consolidate to one correctly-named self-reference.
**PRIORITY 2 — connect_table: customer.household** — add column primary_profile_id (BIGINT) with FK to customer.profile.profile_id — a household should reference its primary/head-of-household customer profile for clienteling and segmentation.
**PRIORITY 3 — connect_table: product.compliance** — add column gtin_registry_id (BIGINT) with FK to product.gtin_registry.gtin_registry_id — product compliance records (e.g., regulatory/cert data) are tracked at the GTIN level, not only SKU.
**PRIORITY 4 — connect_table: promotion.promo_calendar** — add column promo_campaign_id (BIGINT) with FK to promotion.promo_campaign.promo_campaign_id — promo_calendar is heavily referenced but the calendar should anchor to its governing campaign for bidirectional traceability. (Note: promo_calendar is otherwise a valid reference table; verify it is not isolated — it has inbound FKs so this is enrichment only.)
**PRIORITY 5 — connect_table: order.payment** — add column digital_payment_id (BIGINT) with FK to ecommerce.digital_payment.digital_payment_id — order payments captured through the e-commerce checkout flow must link to the originating digital payment record for reconciliation.
**PRIORITY 6 — connect_table: returns.exchange_order** — add column rma_id (BIGINT) with FK to returns.rma.rma_id — an exchange order is initiated from a return authorization and must reference the originating RMA, not only the refund.
**PRIORITY 7 — connect_table: inventory.cycle_count** — add column lot_id (BIGINT) with FK to inventory.lot.lot_id — cycle counts for lot-tracked merchandise must capture the counted lot for shrinkage and expiry accuracy.
**PRIORITY 8 — connect_table: product.assortment** — add column item_hierarchy_id (BIGINT) with FK to product.item_hierarchy.item_hierarchy_id — assortment planning is performed at category/hierarchy level, not only at SKU level.
**PRIORITY 9 — connect_table: store.fixture** — add column item_hierarchy_id (BIGINT) with FK to product.item_hierarchy.item_hierarchy_id — fixtures/planogram positions are assigned to merchandise categories for visual merchandising standards.
**PRIORITY 10 — connect_table: supplier.vendor_scorecard** — add column chargeback_id (BIGINT) with FK to supplier.chargeback.chargeback_id — vendor compliance penalties (chargebacks) are a core scorecard input and must be linkable.
**PRIORITY 11 — connect_table: customer.account** — add column corporate_account_id (BIGINT) with FK to customer.corporate_account.corporate_account_id — B2B sub-accounts must roll up to their parent corporate account; currently the FK direction only exists corporate_account→account.
**PRIORITY 12 — connect_table: order.subscription** — add column order_header_id (BIGINT) with FK to order.header.header_id — each subscription recurrence generates an order; the subscription should reference its most recent/originating order header.
**PRIORITY 13 — connect_table: promotion.circular_ad_placement** — add column product_sku_id (BIGINT) with FK to product.sku.sku_id — a circular ad placement features specific SKUs; without a product link the placement cannot drive promo execution.
**PRIORITY 14 — connect_table: supplychain.warehouse_zone** — add column inventory_node_id (BIGINT) with FK to inventory.inventory_node.inventory_node_id — warehouse zones map to inventory nodes for stock-position granularity; currently only linked to dc_facility.
**PRIORITY 15 — connect_table: store.cluster_membership** — add column region_id (BIGINT) with FK to store.region.region_id — cluster membership analysis requires regional roll-up consistent with cluster's own region FK.
**PRIORITY 16 — connect_table: ecommerce.product_review** — add column profile_id is present; add column verified_purchase_order_line_id is covered — instead add column digital_catalog_id (BIGINT) with FK to ecommerce.digital_catalog.digital_catalog_id — reviews attach to the catalog entry displayed online.
**PRIORITY 17 — connect_table: fulfillment.carrier_service** — add column vendor_id (BIGINT) with FK to supplier.vendor.vendor_id — carrier services are provided by carrier vendors; the link exists only on carrier, but service-level vendor attribution supports cost allocation.
**PRIORITY 18 — connect_table: pricing.price_list** — add column price_zone_id (BIGINT) with FK to pricing.price_zone.price_zone_id — price lists are scoped to price zones for zone-based pricing governance.
**PRIORITY 19 — connect_table: product.brand** — add column item_hierarchy_id (BIGINT) with FK to product.item_hierarchy.item_hierarchy_id — brands are associated with merchandise categories for assortment and category management.
**PRIORITY 20 — connect_table: supplier.edi_config** — add column dc_facility_id (BIGINT) with FK to supplychain.dc_facility.dc_facility_id — EDI integration is configured per receiving facility for inbound document routing.

Other known issues from static analysis (14):
  - Table inventory.replenishment_order has 2 FK columns pointing to inventory.inventory_node (inventory_node_id, primary_replenishment_inventory_node_id) (generic/unlabeled: inventory_node_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to inventory.inventory_node (inventory_node_id, primary_stock_inventory_node_id) (generic/unlabeled: inventory_node_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table customer.account has 2 FK columns pointing to customer.address (address_id, shipping_address_id) (generic/unlabeled: address_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table customer.corporate_account has 2 FK columns pointing to customer.address (address_id, shipping_address_id) (generic/unlabeled: address_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table product.item_variant has 2 FK columns pointing to product.sku (sku_id, target_item_sku_id) (generic/unlabeled: sku_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table fulfillment.fulfillment_order has 2 FK columns pointing to customer.profile (profile_id, order_fulfillment_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table fulfillment.fulfillment_order has 2 FK columns pointing to order.header (header_id, primary_fulfillment_order_header_id) (generic/unlabeled: header_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table returns.disposition has 2 FK columns pointing to supplier.vendor (vendor_id, disposition_vendor_id) (generic/unlabeled: vendor_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supplychain.inbound_shipment has 2 FK columns pointing to supplychain.dc_facility (dc_facility_id, primary_inbound_dc_facility_id) (generic/unlabeled: dc_facility_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supplychain.outbound_order has 2 FK columns pointing to supplychain.dc_facility (dc_facility_id, primary_outbound_dc_facility_id) (generic/unlabeled: dc_facility_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supplychain.outbound_order_line has 2 FK columns pointing to store.location (location_id, source_location_id) (generic/unlabeled: location_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 5 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 36 attribute(s) match person-data patterns but lack pii_ tags
  - PK columns use 3 different datatypes: BIGINT, DECIMAL(18,2), DECIMAL(9,4). Recommend all PKs use BIGINT.

Deterministic score: 97.4/100 (LLM assessment: 82/100)
