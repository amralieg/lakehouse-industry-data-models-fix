**Model Quality Score: 74.01/100**

**PRIORITY 1 — connect_table: guest.address** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — addresses captured at drive-thru/dine-in and delivery must associate to the originating restaurant unit for geolocation and delivery-zone analytics.
**PRIORITY 2 — connect_table: menu.recipe_ingredient** — add column stock_item_id (BIGINT) with FK to inventory.stock_item.stock_item_id — recipe ingredients must resolve to inventory SKUs for accurate COGS and depletion, consistent with menu.menu_modifier already linking stock_item.
**PRIORITY 3 — connect_table: supply.invoice** — add column supplier_id (BIGINT) with FK to supply.supplier.supplier_id — supplier invoices must reference the billing supplier directly rather than only via purchase_order for spend analytics.
**PRIORITY 4 — connect_table: inventory.vendor_item** — add column supplier_id (BIGINT) with FK to supply.supplier.supplier_id — a vendor_item should reference the supplier directly, not only the supplier_contract, to support multi-contract catalog reuse.
**PRIORITY 5 — connect_table: supply.ingredient** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — ingredient master should scope to brand for brand-specific sourcing standards; currently ingredient has zero outbound FKs.
**PRIORITY 6 — connect_table: menu.combo_component** — add column modifier_group_id (BIGINT) with FK to menu.modifier_group.modifier_group_id — combo components frequently expose modifier choices and must link to their applicable modifier group.
**PRIORITY 7 — connect_table: order.channel** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — channel availability/config varies per unit and must be tied to the unit for channel-level SOS reporting.
**PRIORITY 8 — connect_table: workforce.labor_forecast** — add column shift_id (BIGINT) with FK to workforce.shift.shift_id — labor forecasts drive shift planning and should connect to the resulting shift; currently forecast links only to offer/menu/unit which are weak business links.
**PRIORITY 9 — remove_fk: workforce.labor_forecast** — remove FK on column offer (loyalty.offer) because a labor forecast has no direct business dependency on a loyalty offer; this is a spurious cross-domain link.
**PRIORITY 10 — remove_fk: loyalty.reward** — remove FK on column supplier (supply.supplier) because a loyalty reward should reference a menu item or catalog entity, not a supply-chain supplier; this link is not business-meaningful.
**PRIORITY 11 — remove_fk: guest.complaint** — remove FK on column supply.ingredient_lot because complaint traceability to a specific ingredient lot belongs on the quality_inspection/waste_log path, not directly on the guest complaint; over-linking dilutes the guest domain.
**PRIORITY 12 — connect_table: restaurant.location_profile** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — location profiles carry brand-format attributes and should scope to brand in addition to unit.
**PRIORITY 13 — connect_table: order.tax** — add column payment_id (BIGINT) with FK to order.payment.payment_id — tax lines are settled through payments and should reference the payment for reconciliation.
**PRIORITY 14 — connect_table: supply.supplier_ingredient_catalog** — add column supplier_contract_id (BIGINT) with FK to supply.supplier_contract.supplier_contract_id — catalog pricing is governed by contracts and must link to the active contract.
**PRIORITY 15 — connect_table: menu.item_listing** — add column channel_id (BIGINT) with FK to order.channel.channel_id — item listings vary by channel (DT, OLO, 3PD) and must reference the channel for channel-specific menu engineering.
**PRIORITY 16 — connect_table: inventory.food_cost_period** — add column stock_location_id (BIGINT) with FK to inventory.stock_location.stock_location_id — food cost periods are computed per stock location and should reference it for granular COGS%.
**PRIORITY 17 — connect_table: menu.modifier_group** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — modifier groups are brand-governed menu constructs and currently link only to workforce.employee, an incomplete business anchor.
**PRIORITY 18 — connect_table: workforce.certification** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — certifications (ServSafe) are tracked per unit for compliance staffing ratios.
**PRIORITY 19 — connect_table: restaurant.operating_hours** — add column daypart_id (BIGINT) with FK to order.daypart.daypart_id — operating hours align to daypart schedules and should reference the daypart for SOS benchmarking.

Other known issues from static analysis (29):
  - Table menu.recipe_ingredient has 2 FK columns pointing to supply.ingredient (ingredient_id, recipe_main_ingredient_id) (generic/unlabeled: ingredient_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.profile has 2 FK columns pointing to restaurant.unit (unit_id, profile_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.address has 3 FK columns pointing to guest.profile (profile_id, address_profile_id, owner_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.preference has 2 FK columns pointing to guest.profile (profile_id, preference_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.complaint has 2 FK columns pointing to guest.profile (profile_id, complaint_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.interaction has 2 FK columns pointing to guest.profile (profile_id, interaction_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.visit has 2 FK columns pointing to guest.profile (profile_id, visit_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.visit has 2 FK columns pointing to workforce.employee (employee_id, visit_host_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.visit has 2 FK columns pointing to restaurant.unit (unit_id, visit_guest_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.refund has 3 FK columns pointing to workforce.employee (employee_id, refund_employee_id, tertiary_refund_voided_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.receiving_order has 2 FK columns pointing to workforce.employee (employee_id, receiving_manager_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.receiving_order has 2 FK columns pointing to restaurant.unit (unit_id, receiving_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to restaurant.unit (unit_id, origin_restaurant_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to inventory.stock_location (stock_location_id, origin_stock_location_id) (generic/unlabeled: stock_location_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to workforce.employee (employee_id, tertiary_stock_received_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.adjustment has 2 FK columns pointing to workforce.employee (employee_id, primary_inventory_adjusted_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.adjustment has 2 FK columns pointing to inventory.receiving_order (receiving_order_id, adjustment_inventory_related_receiving_receiving_order_id) (generic/unlabeled: receiving_order_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.adjustment has 2 FK columns pointing to inventory.stock_transfer (stock_transfer_id, adjustment_inventory_related_transfer_stock_transfer_id) (generic/unlabeled: stock_transfer_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.food_cost_period has 2 FK columns pointing to workforce.employee (employee_id, tertiary_food_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.employee has 3 FK columns pointing to restaurant.unit (unit_id, employee_unit_id, employee_work_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.shift has 2 FK columns pointing to workforce.employee (employee_id, shift_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.schedule has 2 FK columns pointing to workforce.employee (employee_id, schedule_manager_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 2 FK columns pointing to guest.profile (profile_id, member_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 3 FK columns pointing to restaurant.unit (unit_id, member_unit_id, primary_member_preferred_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.points_ledger has 2 FK columns pointing to order.guest_order (guest_order_id, source_transaction_guest_order_id) (generic/unlabeled: guest_order_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.redemption has 2 FK columns pointing to restaurant.unit (unit_id, redemption_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 20 attribute(s) match person-data patterns but lack pii_ tags
  - 18 non-PK attributes redundantly prefixed with their product name
  - PK columns use 2 different datatypes: BIGINT, DECIMAL(10,4). Recommend all PKs use BIGINT.

Deterministic score: 74.01/100 (LLM assessment: 82/100)
