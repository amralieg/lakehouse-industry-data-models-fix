**Model Quality Score: 96.0/100**

**PRIORITY 1 — rename_attribute: billing.rate_schedule** — rename column superseded_by_rate_schedule_billing_rate_schedule_id to superseded_by_rate_schedule_id because the embedded table-name fragment "billing_rate_schedule" is redundant and breaks the FK naming convention.
**PRIORITY 2 — connect_table: treatment.chemical** — add column regulatory_requirement_id (BIGINT) with FK to compliance.regulatory_requirement.regulatory_requirement_id — chemicals used in treatment are subject to regulatory dosing/usage requirements and the table currently has zero outbound FKs.
**PRIORITY 3 — connect_table: treatment.water_source** — add column water_system_id (BIGINT) with FK to quality.water_system.water_system_id — water sources feed water systems and the table currently has zero outbound FKs, leaving it disconnected on the outbound side.
**PRIORITY 4 — connect_table: asset.class** — add column parent_class_id (BIGINT) with FK to asset.class.asset_class_id — asset classes are hierarchical (equipment categories/subcategories) and the table currently has zero outbound FKs.
**PRIORITY 5 — connect_table: billing.payment_plan** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — payment plans belong to a customer account, not only a billing account, to align with the SSOT customer identity.
**PRIORITY 6 — connect_table: billing.billing_account** — add column premise_id (BIGINT) with FK to customer.premise.premise_id — billing accounts are tied to a serviced premise for consumption-based billing.
**PRIORITY 7 — connect_table: distribution.dma** — add column pump_station_id (BIGINT) with FK to distribution.pump_station.pump_station_id — DMAs are typically fed/controlled by a pump station, and dma currently links only to pressure_zone.
**PRIORITY 8 — connect_table: metering.read_route** — add column pressure_zone_id (BIGINT) with FK to distribution.pressure_zone.pressure_zone_id — read routes span geographic distribution zones for route optimization.
**PRIORITY 9 — connect_table: treatment.facility** — add column compliance_permit_id (BIGINT) with FK to compliance.compliance_permit.compliance_permit_id — facilities operate under compliance permits and facility currently has only a single outbound FK to water_system.
**PRIORITY 10 — connect_table: customer.contact** — add column service_address_id (BIGINT) with FK to customer.service_address.service_address_id — contacts are often associated with a specific service address for notifications.
**PRIORITY 11 — connect_table: wastewater.sewershed_basin** — add column pressure_zone_id (BIGINT) with FK to distribution.pressure_zone.pressure_zone_id — sewershed basins map to distribution service areas; basin currently links only to wwtp.
**PRIORITY 12 — connect_table: treatment.chemical_dose_event** — add column water_source_id (BIGINT) with FK to treatment.water_source.water_source_id — dosing events apply to source/treatment water streams for CT compliance traceability.
**PRIORITY 13 — connect_table: distribution.flow_reading** — add column pressure_zone_id (BIGINT) with FK to distribution.pressure_zone.pressure_zone_id — flow readings occur within pressure zones for NRW/UFW analysis.
**PRIORITY 14 — connect_table: metering.meter** — add column installation_id (BIGINT) with FK to metering.installation.installation_id — link current meter to its active installation for direct meter-to-install traceability.
**PRIORITY 15 — connect_table: billing.rate_schedule** — add column compliance_permit_id (BIGINT) with FK to compliance.compliance_permit.compliance_permit_id — rate schedules are subject to regulatory approval/permit conditions.
**PRIORITY 16 — connect_table: quality.contaminant** — add column water_system_id (BIGINT) with FK to quality.water_system.water_system_id — contaminants are monitored per water system for MCL tracking.

Other known issues from static analysis (20):
  - Table customer.contact has 2 FK columns pointing to customer.customer_account (customer_account_id, contact_customer_customer_account_id) (generic/unlabeled: customer_account_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table customer.complaint has 2 FK columns pointing to customer.person (person_id, complaint_customer_reported_by_person_id) (generic/unlabeled: person_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table distribution.hydrant has 2 FK columns pointing to distribution.pipe_main (pipe_main_id, hydrant_pipe_main_id) (generic/unlabeled: pipe_main_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table billing.payment has 2 FK columns pointing to billing.invoice (invoice_id, payment_invoice_id) (generic/unlabeled: invoice_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table wastewater.wwtp has 2 FK columns pointing to asset.registry (registry_id, wwtp_registry_id) (generic/unlabeled: registry_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table asset.acquisition has 2 FK columns pointing to asset.registry (registry_id, acquisition_registry_id) (generic/unlabeled: registry_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Attribute wastewater.sewershed_basin.amount has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.sewershed_basin_description has unrecognized data type 'TEXT' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.latitude has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.longitude has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.max_value has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.measured_value has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.min_value has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.notes has unrecognized data type 'TEXT' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.percent_value has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.quantity has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - Attribute wastewater.sewershed_basin.target_value has unrecognized data type 'NUMERIC' — expected a valid Spark SQL type (STRING, BIGINT, INT, DECIMAL, BOOLEAN, DATE, TIMESTAMP, etc.)
  - 51 attribute(s) match person-data patterns but lack pii_ tags
  - 9 non-PK attributes redundantly prefixed with their product name
  - PK columns use 2 different datatypes: BIGINT, DECIMAL(18,6). Recommend all PKs use BIGINT.

Deterministic score: 96.0/100 (LLM assessment: 82/100)
