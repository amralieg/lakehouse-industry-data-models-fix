**Model Quality Score: 94.8/100**

**Static Analysis Findings (2 actionable):**
  - [SA:denormalized_natural_key] Product 'encounter.readmission' has both FK 'drg_id' and natural key 'drg_code' for entity 'drg'. The natural key is redundant — use the FK join instead.
  - [SA:denormalized_natural_key] Product 'claim.diagnosis_link' has both FK 'diagnosis_id' and natural key 'diagnosis_code' for entity 'diagnosis'. The natural key is redundant — use the FK join instead.

**PRIORITY 1 — connect_table: pharmacy.lasa_pair** — add column reference_ndc_drug (BIGINT) with FK to reference.ndc_drug.ndc_drug_id — LASA (look-alike-sound-alike) pairs must reference the standardized NDC code set for the confused drug, not only the internal drug_master, to align with reference SSOT.
**PRIORITY 2 — connect_table: provider.specialty** — add column parent_specialty_id (BIGINT) with FK to provider.specialty.specialty_id — specialty taxonomy is hierarchical (sub-specialties roll up to parent specialties) and currently only links outward to reference.snomed_concept.
**PRIORITY 3 — connect_table: patient.emergency_contact** — add column address_id (BIGINT) with FK to patient.address.address_id — emergency contacts require a physical/mailing address for notification, which is currently unmodeled.
**PRIORITY 4 — connect_table: patient.guarantor** — add column address_id (BIGINT) with FK to patient.address.address_id — guarantor billing requires an associated address for statements, currently missing.
**PRIORITY 5 — connect_table: provider.org_provider** — add column address_id (BIGINT) with FK to patient.address.address_id — organizational providers require service/billing location addressing referenced by claims and remittance.
**PRIORITY 6 — connect_table: laboratory.instrument** — add column drug_master reference removed; instead add column manufacturer_org_provider (BIGINT) with FK to provider.org_provider.org_provider_id — instrument maintenance/calibration vendor linkage is needed; currently instrument only links to one org_provider for owning lab.
**PRIORITY 7 — connect_table: billing.payment** — add column claim_id (BIGINT) with FK to claim.claim.claim_id — payments tied to claim adjudication need direct claim linkage; currently payment links only to remittance, not the originating claim.
**PRIORITY 8 — connect_table: claim.remittance** — add column claim_id (BIGINT) with FK to claim.claim.claim_id — remittance advice (ERA) must link to the claim it adjudicates; currently remittance links only to payer/contract/org, leaving the claim association implicit via remittance_line only.
**PRIORITY 9 — connect_table: clinical.immunization** — add column lab_order reference; specifically add column drug_master already present — instead add column reference_loinc_code (BIGINT) with FK to reference.loinc_code.loinc_code_id — immunization observations/titers use LOINC codes for documentation consistency with other clinical observations.
**PRIORITY 10 — connect_table: encounter.bed_assignment** — add column schedulable_resource_id (BIGINT) with FK to scheduling.schedulable_resource.schedulable_resource_id — bed/room assignment should reference the schedulable resource record rather than only open_slot for capacity tracking.
**PRIORITY 11 — connect_table: pharmacy.inventory** — add column dispense_event_id (BIGINT) with FK to pharmacy.dispense_event.dispense_event_id — inventory decrements are driven by dispense events; this consumption linkage is currently absent.
**PRIORITY 12 — connect_table: clinical.observation** — add column provider_clinician (BIGINT) with FK to provider.clinician.clinician_id — observations require a recording/performing clinician for attribution; currently observation has no provider FK.
**PRIORITY 13 — connect_table: provider.board_certification** — add column reference_npi_registry (BIGINT) with FK to reference.npi_registry.npi_registry_id — board certifications are tied to the provider NPI for verification consistency with other credential records.
**PRIORITY 14 — connect_table: reference.crosswalk** — add column source_code_set_version_id (BIGINT) with FK to reference.code_set_version.code_set_version_id — a crosswalk maps between two code set versions; only one code_set_version FK exists, so the second (target) version mapping is undocumented.
**PRIORITY 15 — rename_attribute: reference.crosswalk** — rename column code_set_version to target_code_set_version_id — clarifies the existing single version reference as the target side of the mapping once the source side is added.
**PRIORITY 16 — connect_table: patient.mrn_crosswalk** — add column registration_event_id (BIGINT) with FK to patient.registration_event.registration_event_id — MRN assignment originates during registration; this provenance link is missing.
**PRIORITY 17 — connect_table: encounter.visit** — add column patient_demographics (BIGINT) with FK to patient.demographics.demographics_id — visit links to mpi_record but not to the demographics snapshot used by sibling tables (visit_diagnosis, adt_event); add for consistent demographic resolution.
**PRIORITY 18 — connect_table: insurance.benefit** — add column health_plan_id (BIGINT) with FK to insurance.health_plan.health_plan_id — benefits belong to a health plan; benefit currently only links to coverage code references and form_template, leaving plan ownership unmodeled.
**PRIORITY 19 — connect_table: clinical.care_team_member** — add column provider_org_provider (BIGINT) with FK to provider.org_provider.org_provider_id — care team members may be organizational entities (home health agency, SNF); only individual clinician is linked.
**PRIORITY 20 — connect_table: order.routing** — add column clinician_id (BIGINT) with FK to provider.clinician.clinician_id — order routing targets a receiving clinician/recipient; only org_provider is linked currently.
**PRIORITY 21 — connect_table: pharmacy.formulary** — add column payer_id (BIGINT) with FK to insurance.payer.payer_id — formularies are payer/plan-specific; formulary links to health_plan but not the owning payer for tiering consistency.
**PRIORITY 22 — connect_table: laboratory.reference_range** — add column patient_demographics (BIGINT) removed concept; instead add column snomed_concept already absent — add column reference_snomed_concept (BIGINT) with FK to reference.snomed_concept.snomed_concept_id — reference ranges vary by clinical condition/specimen type expressed in SNOMED; currently only LOINC and instrument are linked.

Other known issues from static analysis (26):
  - Table patient.registration_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_registration_pcp_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table patient.consent_reference has 2 FK columns pointing to consent.record (consent_domain_record_id, record_id) (generic/unlabeled: record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.visit_provider has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_visit_supervising_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table encounter.discharge_summary has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_discharge_follow_up_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.procedure_event has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_procedure_anesthesia_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.problem has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_problem_last_updated_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table clinical.care_team has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_care_member_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.clinical_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_clinical_authorizing_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.referral_order has 2 FK columns pointing to order.clinical_order (clinical_order_id, source_clinical_order_id) (generic/unlabeled: clinical_order_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.referral_order has 2 FK columns pointing to provider.clinician (clinician_id, receiving_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.reconciliation has 2 FK columns pointing to provider.clinician (clinician_id, reconciliation_performed_by_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to patient.mpi_record (mpi_record_id, primary_prescription_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.prescription has 2 FK columns pointing to provider.clinician (clinician_id, primary_prescription_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.lab_order has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_lab_cancelled_by_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table laboratory.test_result has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_test_ordering_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table billing.invoice has 2 FK columns pointing to insurance.payer (payer_id, tertiary_payer_id) (generic/unlabeled: payer_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table claim.eligibility has 2 FK columns pointing to patient.mpi_record (mpi_record_id, eligibility_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table claim.cob has 2 FK columns pointing to insurance.payer (payer_id, tertiary_payer_id) (generic/unlabeled: payer_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table insurance.dependent has 2 FK columns pointing to patient.mpi_record (mpi_record_id, dependent_mpi_record_id) (generic/unlabeled: mpi_record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table consent.treatment_consent has 2 FK columns pointing to provider.clinician (clinician_id, tertiary_treatment_performing_provider_clinician_id) (generic/unlabeled: clinician_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table consent.disclosure_log has 2 FK columns pointing to consent.record (record_id, primary_disclosure_consent_record_id) (generic/unlabeled: record_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table pharmacy.lasa_pair has 2 FK columns pointing to pharmacy.drug_master (drug_master_id, primary_lasa_partner_drug_master_id) (generic/unlabeled: drug_master_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Subdomain 'patient_encounters' appears in 2 domains: clinical, encounter. Consider renaming to qualified form.
  - Subdomain 'order_management' appears in 2 domains: laboratory, order. Consider renaming to qualified form.
  - 7 attribute(s) contain banned boilerplate phrases (Fortune N, multinational, enterprise-wide, cross-company, group reporting). Clean descriptions.
  - 13 attribute(s) match person-data patterns but lack pii_ tags

Deterministic score: 94.8/100 (LLM assessment: 87/100)
