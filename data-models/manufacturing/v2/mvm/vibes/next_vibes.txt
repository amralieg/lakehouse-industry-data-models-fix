**Model Quality Score: 93/100**

**Static Analysis Findings (5 actionable):**
  - [SA:unlinked_fk] Column customer.customer_account.billing_account_id looks like an FK but has no foreign_key_to reference
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'asset.asset_plant' and 'production.production_plant' have the same entity name 'plant' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'asset.asset_plant' and 'supply.supply_plant' have the same entity name 'plant' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'production.production_plant' and 'supply.supply_plant' have the same entity name 'plant' in different domains. One should be the SSOT owner, the other should reference via FK.
  - [SA:cross_domain_duplicate] Potential SSOT violation: 'sales.sales_contract' and 'service.service_contract' have the same entity name 'contract' in different domains. One should be the SSOT owner, the other should reference via FK.

**PRIORITY 1 — connect_table: service.engineer** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — engineer is ISOLATED (zero inbound from other tables except field_service_order, zero outbound FKs) and needs organizational context linking to customer or asset domain
**PRIORITY 2 — connect_table: production.production_plant** — add column inventory_plant_id (BIGINT) with FK to inventory.inventory_plant.inventory_plant_id — production_plant only links to asset.asset_plant but should connect to inventory_plant for material flow alignment
**PRIORITY 3 — connect_table: supply.supply_plant** — add column asset_plant_id (BIGINT) with FK to asset.asset_plant.asset_plant_id — supply_plant only references order.delivery which is semantically weak; it needs a link to the physical plant entity
**PRIORITY 4 — connect_table: supply.supply_plant** — add column inventory_plant_id (BIGINT) with FK to inventory.inventory_plant.inventory_plant_id — supply planning plant must connect to inventory plant for MRP stock visibility
**PRIORITY 5 — remove_fk: supply.supply_plant** — remove FK on column delivery_id because a supply plant is a master data entity and should not reference a transactional delivery record as its only relationship
**PRIORITY 6 — connect_table: inventory.inventory_plant** — add column asset_plant_id (BIGINT) with FK to asset.asset_plant.asset_plant_id — inventory_plant has no outbound FKs and should reference the physical plant entity for location alignment
**PRIORITY 7 — connect_table: order.order_line** — add column header_id (BIGINT) with FK to order.header.header_id — order lines must reference their parent order header
**PRIORITY 8 — connect_table: order.schedule_line** — add column header_id (BIGINT) with FK to order.header.header_id — schedule lines need traceability to the order header for fulfillment tracking
**PRIORITY 9 — rename_product: order.header** — rename to order_header because "header" is too generic and ambiguous outside the order domain context
**PRIORITY 10 — connect_table: billing.billing_account** — add column customer_contact_id (BIGINT) with FK to customer.contact.contact_id — billing accounts need a billing contact for invoice correspondence
**PRIORITY 11 — connect_table: production.production_plant** — add column production_line_id (BIGINT) with FK to production.production_line.production_line_id — production plant should have a default or primary line reference for scheduling
**PRIORITY 12 — connect_table: service.engineer** — add column asset_plant_id (BIGINT) with FK to asset.asset_plant.asset_plant_id — field service engineers need home plant assignment for dispatch optimization
**PRIORITY 13 — connect_table: product.family** — add column product_specification_id (BIGINT) with FK to product.product_specification.product_specification_id — product families should link to their default specification for inheritance
**PRIORITY 14 — connect_table: sales.rep** — add column asset_plant_id (BIGINT) with FK to asset.asset_plant.asset_plant_id — sales reps need territory/plant assignment for regional coverage
**PRIORITY 15 — connect_table: inventory.cycle_count** — add column material_master_id (BIGINT) with FK to inventory.material_master.material_master_id — cycle counts must specify which material is being counted
**PRIORITY 16 — connect_table: supply.order_pegging** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — pegging must link supply to demand including production orders
**PRIORITY 17 — connect_table: procurement.rfq** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — RFQs in manufacturing context need requesting organization reference
**PRIORITY 18 — connect_table: quality.control_plan** — add column product_specification_id (BIGINT) with FK to product.product_specification.product_specification_id — control plans should reference the product specification they enforce
**PRIORITY 19 — rename_attribute: engineering.revision** — rename column primary_superseded_by_revision_engineering_revision_id to superseded_by_revision_id
**PRIORITY 20 — rename_attribute: customer.customer_account** — rename column primary_ultimate_parent_account_customer_account_id to ultimate_parent_account_id
**PRIORITY 21 — rename_attribute: billing.invoice** — rename column primary_credit_note_invoice_id to credit_note_invoice_id
**PRIORITY 22 — rename_attribute: engineering.engineering_specification** — rename column superseded_by_specification_engineering_specification_id to superseded_by_specification_id
**PRIORITY 23 — rename_attribute: production.order_confirmation** — rename column reversed_confirmation_order_confirmation_id to reversed_confirmation_id
**PRIORITY 24 — connect_table: asset.asset_plant** — add column inventory_plant_id (BIGINT) with FK to inventory.inventory_plant.inventory_plant_id — asset_plant already references inventory.inventory_plant per the model but inventory_plant has no reciprocal; verify bidirectional navigability
**PRIORITY 25 — connect_table: quality.inspection_result** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — inspection results during production need direct WO traceability beyond just inspection_lot
**PRIORITY 26 — connect_table: procurement.supplier_invoice** — add column billing_account_id (BIGINT) with FK to billing.billing_account.billing_account_id — supplier invoices should link to the billing domain for AP/AR reconciliation
**PRIORITY 27 — connect_table: procurement.supplier_invoice** — add column inventory_plant_id (BIGINT) with FK to inventory.inventory_plant.inventory_plant_id — supplier invoices need receiving plant reference for three-way match
**PRIORITY 28 — connect_table: production.bom_consumption** — add column run_id (BIGINT) with FK to production.run.run_id — BOM consumption occurs during production runs and needs direct linkage
**PRIORITY 29 — connect_table: quality.customer_complaint** — add column service_request_id (BIGINT) with FK to service.request.request_id — customer complaints typically originate from or escalate to service requests
**PRIORITY 30 — connect_table: billing.credit_limit** — add column customer_account_id (BIGINT) with FK to customer.customer_account.customer_account_id — credit limits need direct customer reference beyond just billing_account for enterprise reporting
**PRIORITY 31 — rename_attribute: asset.asset_work_order** — rename column parent_work_order_asset_work_order_id to parent_work_order_id
**PRIORITY 32 — rename_attribute: asset.asset_plant** — rename column parent_plant_asset_plant_id to parent_plant_id
**PRIORITY 33 — connect_table: supply.mrp_run** — add column inventory_plant_id (BIGINT) with FK to inventory.inventory_plant.inventory_plant_id — MRP runs execute at plant level and need inventory plant context beyond just supply_plant
**PRIORITY 34 — connect_table: order.pricing_condition** — add column header_id (BIGINT) with FK to order.header.header_id — pricing conditions need order header reference for header-level discounts and surcharges
**PRIORITY 35 — connect_table: order.delivery** — add column order_line_id (BIGINT) with FK to order.order_line.order_line_id — deliveries should reference specific order lines for partial shipment tracking
**PRIORITY 36 — connect_table: production.schedule** — add column production_line_id (BIGINT) with FK to production.production_line.production_line_id — production schedules must specify which line is scheduled
**PRIORITY 37 — connect_table: service.service_contract** — add column product_sku_master_id (BIGINT) with FK to product.sku_master.sku_master_id — service contracts cover specific products and need SKU-level granularity
**PRIORITY 38 — connect_table: service.warranty** — add column service_contract_id (BIGINT) with FK to service.service_contract.service_contract_id — warranties often convert to or coexist with service contracts
**PRIORITY 39 — connect_table: quality.certificate_of_conformance** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — CoCs are generated against specific production work orders
**PRIORITY 40 — connect_table: procurement.goods_receipt** — add column production_work_order_id (BIGINT) with FK to production.production_work_order.production_work_order_id — goods receipts from production (finished goods) need WO reference
**PRIORITY 41 — connect_table: asset.calibration_record** — add column pm_schedule_id (BIGINT) with FK to asset.pm_schedule.pm_schedule_id — calibrations are often triggered by PM schedules and need that linkage
**PRIORITY 42 — connect_table: supply.demand_forecast** — add column sales_contract_id (BIGINT) with FK to sales.sales_contract.sales_contract_id — demand forecasts for contracted volumes need contract reference

Other known issues from static analysis (3):
  - Table engineering.engineering_bom_line has 2 FK columns pointing to engineering.component (component_id, tertiary_engineering_substitute_component_id) (generic/unlabeled: component_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table sales.price_book_entry has 2 FK columns pointing to product.catalog_entry (catalog_entry_id, tertiary_price_product_catalog_entry_id) (generic/unlabeled: catalog_entry_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 108 attribute(s) match person-data patterns but lack pii_ tags

Deterministic score: 93/100 (LLM assessment: 62/100)
