**Model Quality Score: 94.2/100**

**PRIORITY 1 — connect_table: supply.supplier** — add column primary_brand_id (BIGINT) with FK to restaurant.brand.brand_id — supplier master must associate to the brand it serves for brand-level spend and sourcing analytics.
**PRIORITY 2 — connect_table: menu.modifier_group** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — modifier groups are brand-scoped and currently only link to workforce.employee, leaving them effectively disconnected from menu/brand context.
**PRIORITY 3 — connect_table: workforce.position** — add column brand_standard_id (BIGINT) with FK to restaurant.brand_standard.brand_standard_id — positions are defined against brand standards but currently only self-reference, leaving them isolated from the restaurant domain.
**PRIORITY 4 — connect_table: order.guest_order** — add column order_channel_id (BIGINT) with FK to order.channel.channel_id — verify channel linkage exists; guest_order references order.channel which is correct, but ensure no dangling channel column duplicates.
**PRIORITY 5 — connect_table: menu.item_assignment** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — item assignments to menus need unit scoping for location-specific menu availability.
**PRIORITY 6 — connect_table: loyalty.tier** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — tiers only link to program; brand context needed for multi-brand loyalty analytics.
**PRIORITY 7 — connect_table: workforce.payroll_record** — add column position_id (BIGINT) with FK to workforce.position.position_id — payroll records must reference the position for pay-rate and labor-cost reconciliation.
**PRIORITY 8 — connect_table: workforce.leave_request** — add column position_id (BIGINT) with FK to workforce.position.position_id — leave requests need position context for coverage planning.
**PRIORITY 9 — connect_table: workforce.schedule** — add column position_id (BIGINT) with FK to workforce.position.position_id — schedules must link to positions for role-based labor forecasting.
**PRIORITY 10 — connect_table: supply.invoice** — add column purchase_order_line_id (BIGINT) with FK to supply.purchase_order_line.purchase_order_line_id — invoice should reconcile to PO line items for three-way match accuracy.
**PRIORITY 11 — connect_table: supply.supplier_ingredient_sourcing** — add column supplier_contract_id (BIGINT) with FK to supply.supplier_contract.supplier_contract_id — sourcing decisions should reference the governing contract.
**PRIORITY 12 — connect_table: restaurant.location_profile** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — location profiles need brand association for brand-level location analytics.
**PRIORITY 13 — connect_table: menu.combo_meal** — add column brand_id (BIGINT) with FK to restaurant.brand.brand_id — combo meals are brand-scoped products and need direct brand linkage.
**PRIORITY 14 — connect_table: guest.demographic** — add column household_id (BIGINT) with FK to guest.household.household_id — demographics should associate to household for segment/household-level analytics.
**PRIORITY 15 — connect_table: loyalty.offer_distribution** — add column unit_id (BIGINT) with FK to restaurant.unit.unit_id — offer distribution events need unit context for location-level campaign attribution.
**PRIORITY 16 — connect_table: foodsafety.sop_document** — add column haccp_plan_id (BIGINT) with FK to foodsafety.haccp_plan.haccp_plan_id — SOPs are typically derived from HACCP plans and should link for compliance traceability.
**PRIORITY 17 — connect_table: menu.nutrition_profile** — add column allergen_declaration_id (BIGINT) with FK to menu.allergen_declaration.allergen_declaration_id — nutrition and allergen data are tightly coupled and should cross-reference.
**PRIORITY 18 — connect_table: restaurant.operating_hours** — add column location_profile_id (BIGINT) with FK to restaurant.location_profile.location_profile_id — operating hours vary by location profile and should reference it.
**PRIORITY 19 — connect_table: workforce.certification** — add column training_completion_id (BIGINT) with FK to workforce.training_completion.training_completion_id — certifications are typically earned via training completion and should link.
**PRIORITY 20 — connect_table: order.payment** — add column catering_order_id (BIGINT) with FK to order.catering_order.catering_order_id — catering orders require payment tracking distinct from standard guest orders.

Other known issues from static analysis (34):
  - Table menu.recipe_ingredient has 2 FK columns pointing to supply.ingredient (ingredient_id, primary_recipe_substitute_ingredient_id) (generic/unlabeled: ingredient_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.profile has 2 FK columns pointing to restaurant.unit (unit_id, profile_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.address has 3 FK columns pointing to guest.profile (profile_id, address_profile_id, owner_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.preference has 2 FK columns pointing to guest.profile (profile_id, preference_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.satisfaction_survey has 2 FK columns pointing to restaurant.unit (unit_id, tertiary_satisfaction_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.complaint has 2 FK columns pointing to guest.profile (profile_id, complaint_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.complaint has 2 FK columns pointing to restaurant.unit (unit_id, complaint_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.interaction has 2 FK columns pointing to guest.profile (profile_id, interaction_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.interaction has 2 FK columns pointing to restaurant.unit (unit_id, interaction_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table guest.demographic has 2 FK columns pointing to guest.profile (profile_id, demographic_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.order_item has 2 FK columns pointing to menu.menu_item (menu_item_id, tertiary_order_menu_item_menu_item_id) (generic/unlabeled: menu_item_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.order_modifier has 4 FK columns pointing to menu.menu_modifier (menu_modifier_id, quaternary_order_menu_modifier_menu_modifier_id, source_modifier_menu_modifier_id, tertiary_order_menu_modifier_id) (generic/unlabeled: menu_modifier_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.refund has 3 FK columns pointing to workforce.employee (employee_id, refund_processed_by_employee_id, tertiary_refund_voided_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table order.refund has 2 FK columns pointing to guest.profile (profile_id, refund_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supply.goods_receipt has 2 FK columns pointing to workforce.employee (employee_id, primary_goods_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supply.goods_receipt_line has 2 FK columns pointing to workforce.employee (employee_id, receiving_user_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table supply.quality_inspection has 2 FK columns pointing to workforce.employee (employee_id, primary_quality_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to restaurant.unit (unit_id, origin_restaurant_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to inventory.stock_location (stock_location_id, origin_stock_location_id) (generic/unlabeled: stock_location_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.stock_transfer has 2 FK columns pointing to workforce.employee (employee_id, tertiary_stock_received_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.replenishment_order has 6 FK columns pointing to workforce.employee (employee_id, quaternary_replenishment_employee_id, receiving_employee_id, receiving_user_employee_id, tertiary_replenishment_cancelled_by_user_employee_id, tertiary_replenishment_created_by_user_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.adjustment has 2 FK columns pointing to workforce.employee (employee_id, primary_inventory_adjusted_by_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table inventory.adjustment has 2 FK columns pointing to inventory.stock_transfer (stock_transfer_id, adjustment_inventory_related_transfer_stock_transfer_id) (generic/unlabeled: stock_transfer_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.employee has 3 FK columns pointing to restaurant.unit (unit_id, employee_unit_id, employee_work_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.shift has 2 FK columns pointing to workforce.employee (employee_id, shift_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table workforce.schedule has 2 FK columns pointing to workforce.employee (employee_id, schedule_manager_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table foodsafety.food_safety_audit has 2 FK columns pointing to workforce.employee (employee_id, primary_food_employee_id) (generic/unlabeled: employee_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 2 FK columns pointing to guest.profile (profile_id, member_profile_id) (generic/unlabeled: profile_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.member has 3 FK columns pointing to restaurant.unit (unit_id, member_unit_id, primary_member_preferred_location_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.points_ledger has 2 FK columns pointing to order.guest_order (guest_order_id, source_transaction_guest_order_id) (generic/unlabeled: guest_order_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - Table loyalty.redemption has 2 FK columns pointing to restaurant.unit (unit_id, redemption_unit_id) (generic/unlabeled: unit_id) — rename generic columns to describe their business role (e.g., inspector_employee_id, approver_employee_id; origin_plan_id, amendment_plan_id)
  - 22 attribute(s) match person-data patterns but lack pii_ tags
  - 18 non-PK attributes redundantly prefixed with their product name
  - PK columns use 3 different datatypes: BIGINT, DECIMAL(18,2), DECIMAL(9,4). Recommend all PKs use BIGINT.

Deterministic score: 94.2/100 (LLM assessment: 78/100)
